[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kudo-ui"
version = "0.0.1"
description = "Experimental Python visualization toolkit (early placeholder)"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  {name = "Manas Maheshwari"}
]

