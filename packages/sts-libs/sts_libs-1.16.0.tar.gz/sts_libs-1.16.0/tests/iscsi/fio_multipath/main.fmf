summary: FIO I/O tests on iSCSI multipath devices
description: |
  Tests that perform login to iSCSI target with multipath enabled,
  run FIO I/O tests on multipath devices, then logout.

  Test steps:
  1. Login to iSCSI targets using iscsiadm
  2. Wait for devices to settle
  3. Check and start multipath service if not running
  4. Discover and get all available multipath devices
  5. Verify that multipath devices are found
  6. Run FIO performance test on each multipath device
  7. Verify FIO test completion and success on all multipath devices
  8. Stop multipath service if it was started by the test
  9. Logout of the iSCSI targets
  10. Log test progress and results throughout
component:
  - kernel
  - iscsi-initiator-utils
  - device-mapper-multipath
environment:
  PACKAGE: iscsi-initiator-utils
tag:
  - fio
  - iscsi-offload
  - multipath
