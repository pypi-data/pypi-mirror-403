[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "degr33"
version = "1.0.1"
description = "Read-only Linux command explanation tool"
readme = "README.md"
requires-python = ">=3.8"

dependencies = [
    "openai>=1.0.0"
]

[project.scripts]
degr33 = "degr33.cli:main"
