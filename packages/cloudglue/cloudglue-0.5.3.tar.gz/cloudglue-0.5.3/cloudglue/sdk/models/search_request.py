# coding: utf-8

"""
    Cloudglue API

    API for Cloudglue

    The version of the OpenAPI document: 0.5.7
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from cloudglue.sdk.models.search_filter import SearchFilter
from cloudglue.sdk.models.search_request_source_image import SearchRequestSourceImage
from typing import Optional, Set
from typing_extensions import Self

class SearchRequest(BaseModel):
    """
    SearchRequest
    """ # noqa: E501
    scope: Optional[StrictStr] = Field(default=None, description="Search scope - 'file' searches at file level (requires collections with enable_summary=true), 'segment' searches at segment level, 'face' searches for faces in videos using image matching")
    collections: Optional[Annotated[List[StrictStr], Field(min_length=1)]] = Field(default=None, description="List of collection IDs to search within.   For text search (scope='file' or 'segment'): All collections must be of collection_type 'media-descriptions' or 'rich-transcripts'. For file-level search, collections must have 'enable_summary: true' in transcribe_config.  For face search (scope='face'): All collections must be of collection_type 'face-analysis'.")
    query: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, description="Text search query to find relevant content (required for scope='file' or 'segment')")
    source_image: Optional[SearchRequestSourceImage] = None
    limit: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=10, description="Maximum number of search results to return (applies to total items across groups when grouping)")
    filter: Optional[SearchFilter] = None
    threshold: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Minimum score threshold to filter results. Can be any real number.")
    group_by_key: Optional[StrictStr] = Field(default=None, description="Group results by file. Cannot be used with scope=\"file\". When specified, results are grouped by file_id.")
    sort_by: Optional[StrictStr] = Field(default='score', description="Sort order for results. Default: \"score\". When group_by_key is specified, can also use \"item_count\" to sort by number of items per group.")
    search_modalities: Optional[Annotated[List[StrictStr], Field(max_length=5)]] = Field(default=None, description="Specifies the type(s) of search to execute. When multiple modalities are specified, a hybrid search is executed that combines results from each modality.  Available modalities: - `general_content`: baseline for matching the content of the search item (file/segment) based on visual or spoken content similarity to provided short natural language query string - `speech_lexical`: performs keyword based search (e.g. query of `president` matching `president` or `presidential` strings) and exact match (e.g. specifically find mentions of `\"Barack Obama\"` or `\"Donald Trump\"`) against speech content present in search item - `ocr_lexical`: performs keyword based search and exact match against screen text content present in search item - `tag_semantic`: performs basic word semantic similarity search against tag values associated with search items (e.g. `query=animal` expected to match tags with value containing `dog` or `cat`) - `tag_lexical`: performs keyword based search and exact match against tag values associated with search items  Only applicable when search `scope=file` or `scope=segment`. ")
    label_filters: Optional[List[StrictStr]] = Field(default=None, description="Filter eligible search items by presence of one or more labels in the provided list (otherwise all tags will be considered in search response). Only supported for `tag_semantic` and `tag_lexical` search modalities ")
    __properties: ClassVar[List[str]] = ["scope", "collections", "query", "source_image", "limit", "filter", "threshold", "group_by_key", "sort_by", "search_modalities", "label_filters"]

    @field_validator('scope')
    def scope_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['file', 'segment', 'face']):
            raise ValueError("must be one of enum values ('file', 'segment', 'face')")
        return value

    @field_validator('group_by_key')
    def group_by_key_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['file']):
            raise ValueError("must be one of enum values ('file')")
        return value

    @field_validator('sort_by')
    def sort_by_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['score', 'item_count']):
            raise ValueError("must be one of enum values ('score', 'item_count')")
        return value

    @field_validator('search_modalities')
    def search_modalities_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['general_content', 'speech_lexical', 'ocr_lexical', 'tag_semantic', 'tag_lexical']):
                raise ValueError("each list item must be one of ('general_content', 'speech_lexical', 'ocr_lexical', 'tag_semantic', 'tag_lexical')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SearchRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of source_image
        if self.source_image:
            _dict['source_image'] = self.source_image.to_dict()
        # override the default output from pydantic by calling `to_dict()` of filter
        if self.filter:
            _dict['filter'] = self.filter.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SearchRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "scope": obj.get("scope"),
            "collections": obj.get("collections"),
            "query": obj.get("query"),
            "source_image": SearchRequestSourceImage.from_dict(obj["source_image"]) if obj.get("source_image") is not None else None,
            "limit": obj.get("limit") if obj.get("limit") is not None else 10,
            "filter": SearchFilter.from_dict(obj["filter"]) if obj.get("filter") is not None else None,
            "threshold": obj.get("threshold"),
            "group_by_key": obj.get("group_by_key"),
            "sort_by": obj.get("sort_by") if obj.get("sort_by") is not None else 'score',
            "search_modalities": obj.get("search_modalities"),
            "label_filters": obj.get("label_filters")
        })
        return _obj


