# coding: utf-8

"""
    Cloudglue API

    API for Cloudglue

    The version of the OpenAPI document: 0.5.7
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from cloudglue.sdk.models.search_response_results_inner import SearchResponseResultsInner
from typing import Optional, Set
from typing_extensions import Self

class SearchResponse(BaseModel):
    """
    SearchResponse
    """ # noqa: E501
    id: StrictStr = Field(description="ID of the search response")
    object: StrictStr = Field(description="Object type, always 'search'")
    query: Optional[StrictStr] = Field(default=None, description="The search query that was executed (for text search) or the source image URL/base64 indicator (for face search)")
    scope: StrictStr = Field(description="The search scope that was used")
    group_by_key: Optional[StrictStr] = Field(default=None, description="The key used for grouping results. Only present when group_by_key was specified in the request and results are grouped.")
    group_count: Optional[StrictInt] = Field(default=None, description="Number of groups in the results. Only present when group_by_key is specified.")
    search_modalities: Optional[Annotated[List[StrictStr], Field(max_length=5)]] = Field(default=None, description="Specifies the type(s) of search to execute. When multiple modalities are specified, a hybrid search is executed that combines results from each modality.  Available modalities: - `general_content`: baseline for matching the content of the search item (file/segment) based on visual or spoken content similarity to provided short natural language query string - `speech_lexical`: performs keyword based search (e.g. query of `president` matching `president` or `presidential` strings) and exact match (e.g. specifically find mentions of `\"Barack Obama\"` or `\"Donald Trump\"`) against speech content present in search item - `ocr_lexical`: performs keyword based search and exact match against screen text content present in search item - `tag_semantic`: performs basic word semantic similarity search against tag values associated with search items (e.g. `query=animal` expected to match tags with value containing `dog` or `cat`) - `tag_lexical`: performs keyword based search and exact match against tag values associated with search items  Only applicable when search `scope=file` or `scope=segment`. ")
    results: List[SearchResponseResultsInner] = Field(description="Array of search results ranked by relevance score")
    total: StrictInt = Field(description="Total number of results returned. When group_by_key is specified, this represents the total number of items across all groups (not the number of groups).")
    limit: StrictInt = Field(description="The limit that was applied to the search")
    __properties: ClassVar[List[str]] = ["id", "object", "query", "scope", "group_by_key", "group_count", "search_modalities", "results", "total", "limit"]

    @field_validator('object')
    def object_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['search']):
            raise ValueError("must be one of enum values ('search')")
        return value

    @field_validator('scope')
    def scope_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['file', 'segment', 'face']):
            raise ValueError("must be one of enum values ('file', 'segment', 'face')")
        return value

    @field_validator('group_by_key')
    def group_by_key_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['file']):
            raise ValueError("must be one of enum values ('file')")
        return value

    @field_validator('search_modalities')
    def search_modalities_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['general_content', 'speech_lexical', 'ocr_lexical', 'tag_semantic', 'tag_lexical']):
                raise ValueError("each list item must be one of ('general_content', 'speech_lexical', 'ocr_lexical', 'tag_semantic', 'tag_lexical')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SearchResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in results (list)
        _items = []
        if self.results:
            for _item_results in self.results:
                if _item_results:
                    _items.append(_item_results.to_dict())
            _dict['results'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SearchResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "object": obj.get("object"),
            "query": obj.get("query"),
            "scope": obj.get("scope"),
            "group_by_key": obj.get("group_by_key"),
            "group_count": obj.get("group_count"),
            "search_modalities": obj.get("search_modalities"),
            "results": [SearchResponseResultsInner.from_dict(_item) for _item in obj["results"]] if obj.get("results") is not None else None,
            "total": obj.get("total"),
            "limit": obj.get("limit")
        })
        return _obj


