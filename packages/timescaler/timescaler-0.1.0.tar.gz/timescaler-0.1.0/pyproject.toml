[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
# Show logs in real-time on the console during test runs.
log_cli = true
# Set the minimum log level to display on the console.
log_cli_level = "INFO"