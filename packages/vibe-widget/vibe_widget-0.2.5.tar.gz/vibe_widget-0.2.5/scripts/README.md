# Scripts

Short entry points for common dev tasks. Run from the repo root.

- `run-tests.sh`: Primary test runner.
- `run-tests-optional.sh`: Extra/slow tests that are safe to skip in quick iterations.
