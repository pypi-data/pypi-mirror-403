"""Auto-generated MCP server.

Generated by auto-mcp.
"""

from mcp.server.fastmcp import FastMCP

import math_utils

# Create MCP server
mcp = FastMCP(name="auto-mcp-server")

# Tools

@mcp.tool(name="add")
def add(a: float, b: float):
    """Add two numbers together."""
    return math_utils.add(a=a, b=b)


@mcp.tool(name="divide")
def divide(a: float, b: float):
    """Divide the first number by the second."""
    return math_utils.divide(a=a, b=b)


@mcp.tool(name="factorial")
def factorial(n: int):
    """Calculate the factorial of a non-negative integer."""
    return math_utils.factorial(n=n)


@mcp.tool(name="gcd")
def gcd(a: int, b: int):
    """Calculate the greatest common divisor of two integers."""
    return math_utils.gcd(a=a, b=b)


@mcp.tool(name="is_prime")
def is_prime(n: int):
    """Check if a number is prime."""
    return math_utils.is_prime(n=n)


@mcp.tool(name="multiply")
def multiply(a: float, b: float):
    """Multiply two numbers together."""
    return math_utils.multiply(a=a, b=b)


@mcp.tool(name="power")
def power(base: float, exponent: float):
    """Raise a number to a power."""
    return math_utils.power(base=base, exponent=exponent)


@mcp.tool(name="subtract")
def subtract(a: float, b: float):
    """Subtract the second number from the first."""
    return math_utils.subtract(a=a, b=b)




if __name__ == "__main__":
    mcp.run()
