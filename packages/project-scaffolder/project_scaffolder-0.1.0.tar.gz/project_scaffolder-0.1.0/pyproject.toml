[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "project-scaffolder"
version = "0.1.0"
description = "Generate project folders and files from a tree-style text file"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{name = "Dipak Pulami Magar", email = "dpm.it24800@gmail.com"}]
keywords = ["scaffold", "project", "tree", "cli"]

[project.scripts]
scaffold = "scaffold.cli:main"
