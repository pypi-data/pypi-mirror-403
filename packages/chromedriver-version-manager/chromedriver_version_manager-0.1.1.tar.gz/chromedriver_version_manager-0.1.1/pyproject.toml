[project]
name = "chromedriver-version-manager"
version = "0.1.1"
description = "Chromedriver Version Manager"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
