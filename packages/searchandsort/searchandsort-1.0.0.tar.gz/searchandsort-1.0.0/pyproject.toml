[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "searchandsort"
version = "1.0.0"
description = "Python package for searching and sorting algorithms with time analysis"
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }
authors = [
    { name = "M P Nithish Praba" }
]

[project.urls]
Homepage = "https://github.com/mpnithishpraba/PP1-Search-and-Sort"
