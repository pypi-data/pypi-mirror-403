[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "subham-resume"
version = "0.1.0"
description = "Subham's resume as a pip package"
authors = [{name="Subham Ghosh"}]
dependencies = []

[project.scripts]
resume = "resume_pkg.main:show_resume"

