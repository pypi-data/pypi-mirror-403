_target_: olympus_core.core.ModuleDatasets    
train:
  _target_: torch.utils.data.ConcatDataset
  _partial_: True
  datasets:
    - _target_: olympus_biomed_parse.datasets.biomedseg_dataset.BiomedSegDataset 
      root_dir: "${mounts.external}/BiomedSeg/PanNuke"
test:
  # _target_: torch.utils.data.ConcatDataset
  # datasets:
  - _target_: olympus_biomed_parse.datasets.biomedseg_dataset.BiomedSegDataset 
    _partial_: True
    root_dir: "${mounts.external}/BiomedSeg/PanNuke"
    split: "test"