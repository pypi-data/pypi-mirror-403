# _target_: olympus_biomed_parse.models.multimodal_models.seem.backbone.focal.D2FocalNet
# pretrain_img_size: 224
# patch_size: 4
# embed_dim: 192
# in_chans: 3
# depths: [2,2,18,2]
# focal_levels: [4,4,4,4]
# focal_windows: [3,3,3,3]
# drop_rate: 0.0
# mlp_ratio: 4.0
# patch_norm: true
# use_conv_embed: true
# use_checkpoint: false
# out_indices: [0,1,2,3]
# scaling_modulator: true
# use_layerscale: true
# out_features: ["res2", "res3", "res4", "res5"]
# use_postln: true
# use_postln_in_modulation: false

#image_encoder:
_target_: azureml.acft.image.components.olympus_biomed_parse.models.multimodal_models.seem.backbone.hiera_image_encoder.SEEMHieraEncoder
scalp: 0
trunk:
  _target_: azureml.acft.image.components.olympus_biomed_parse.external.sam2.modeling.backbones.hieradet.Hiera
  embed_dim: 96
  num_heads: 1
  stages: [1, 2, 11, 2]
  global_att_blocks: [7, 10, 13]
  window_pos_embed_bkg_spatial_size: [7, 7]
neck:
  _target_: azureml.acft.image.components.olympus_biomed_parse.external.sam2.modeling.backbones.image_encoder.FpnNeck
  position_encoding:
    _target_: azureml.acft.image.components.olympus_biomed_parse.external.sam2.modeling.position_encoding.PositionEmbeddingSine
    num_pos_feats: 256
    normalize: true
    temperature: 10000
  d_model: 256
  backbone_channel_list: [768, 384, 192, 96]
  fpn_top_down_levels: [2, 3]
  fpn_interp_model: nearest
ckpt_path: ${mounts.external}/data/PretrainedModels/sam2/sam2_hiera_small.pt