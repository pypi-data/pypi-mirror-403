# This is a minimal Python file with no classes or functions
# It only contains imports, comments, and simple statements

import os
import sys

# Just some module-level variables
DEBUG = True
CONFIG_PATH = "/etc/myapp/config.ini"

# A simple assignment
result = 42 + 8

# Some string operations
message = "Hello, World!"
formatted = f"The result is: {result}"

# Print statement
print(formatted)
