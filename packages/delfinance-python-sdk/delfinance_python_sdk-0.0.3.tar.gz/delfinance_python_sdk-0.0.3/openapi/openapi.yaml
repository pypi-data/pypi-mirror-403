openapi: 3.0.0
info:
  title: Delfinance SDK API
  description: Specification for the Delfinance SDKs.
  version: 1.0.0
paths:
  /baas/api/v2/transfers/{transferIdentifier}:
    get:
      summary: Get Transfer
      operationId: getTransfer
      parameters:
        - name: transferIdentifier
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Successful response
