[project]
name = "mainzelliste-client"
version = "0.1.1"
description = "Client for Mainzelliste, a pseudonymization service."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.0.2",
    "requests>=2.32.5",
]
