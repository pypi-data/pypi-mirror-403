jobs:
  test-user-registered-step:
    description: Run test with a user-registered step
    steps:
      - name: Run user-defined step
        uses: app-name/run-me
        with:
          input: input_file.inp
          output: result.out

      - name: Run user-defined action
        uses: app-name/custom-step
        with:
          input: input_file.inp
          output: result.out
