[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "datacontracts"
version = "0.1.3"
description = "datacontracts v0.1.3 lets you define business-friendly data rules and reports every invalid row with clear messages."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }

authors = [
    { name = "M.Umer Farooq" }
]

dependencies = [
    "pandas"
]

classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: MIT License",
]

[project.urls]
Homepage = "https://github.com/ben-slates/datacontracts"
Repository = "https://github.com/ben-slates/datacontracts"
Issues = "https://github.com/ben-slates/datacontracts/issues"
