# =============================================================================
# Keycloak Environment Variables - Example File
# =============================================================================
# 
# Este archivo muestra todas las variables disponibles para configurar Keycloak.
# Copia este archivo según tu escenario:
#
#   cp .env.development .env    # Para desarrollo local
#   cp .env.azure .env          # Para Azure Web Apps
#   cp .env.onpremise .env      # Para on-premise con proxy
#
# O consulta los archivos específicos:
#   - .env.development  : Desarrollo local con H2
#   - .env.azure        : Azure Web Apps con PostgreSQL
#   - .env.onpremise    : On-premise con Traefik + PostgreSQL
#
# =============================================================================

# -----------------------------------------------------------------------------
# Deployment Configuration
# -----------------------------------------------------------------------------
KEYCLOAK_VERSION=26.4.5
DEPLOYMENT_MODE=development  # development | development-db | azure | onpremise

# -----------------------------------------------------------------------------
# Admin Credentials
# -----------------------------------------------------------------------------
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=admin

# API Client Secret
KEYCLOAK_API_CLIENT_SECRET=dev-secret

# -----------------------------------------------------------------------------
# HTTP Configuration
# -----------------------------------------------------------------------------
KC_HTTP_PORT=8090        # 8090 para dev/onpremise, 80 para Azure
KC_LOG_LEVEL=INFO        # DEBUG | INFO | WARN | ERROR

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# NOTA: Los modos requieren diferentes configuraciones de BD:
#   - development: H2 en memoria (sin configuración adicional)
#   - development-db: PostgreSQL en Docker o externa
#   - azure: PostgreSQL externa (Azure Database for PostgreSQL)
#   - onpremise: PostgreSQL externa

KC_DB=postgres                           # postgres | mysql
KC_DRIVER=postgresql                     # postgresql | mysql (para JDBC URL)
KC_DB_URL_HOST=                          # hostname o IP del servidor de BD
KC_DB_URL_PORT=5432                      # puerto de la BD
KC_DB_URL_DATABASE=keycloak              # nombre de la base de datos
KC_DB_USERNAME=                          # usuario de la BD
KC_DB_PASSWORD=                          # contraseña de la BD

# -----------------------------------------------------------------------------
# Hostname Configuration (para azure/onpremise con proxy)
# -----------------------------------------------------------------------------
KC_HOSTNAME=                             # ej: https://example.com
KC_HOSTNAME_PATH=/                       # ej: /keycloak o /
KC_PROXY_HEADERS=xforwarded              # xforwarded | forwarded

# -----------------------------------------------------------------------------
# Traefik Configuration (solo para onpremise)
# -----------------------------------------------------------------------------
TRAEFIK_LOG_LEVEL=INFO                   # DEBUG | INFO | WARN | ERROR


