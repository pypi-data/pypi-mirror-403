#include "MotorCmd.idl"

module booster {
    module msg {
        enum CmdType {
            PARALLEL,
            SERIAL
        };

        struct LowCmd {
            CmdType cmd_type;
            sequence<MotorCmd> motor_cmd;
        };
    };
};
