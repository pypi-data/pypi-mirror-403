#include "MotorCmd.idl"

module booster_interface {
    module msg {
        enum CmdType {
            PARALLEL,
            SERIAL
        };

        struct LowCmd {
            CmdType cmd_type;
            sequence<MotorCmd> motor_cmd;
        };
    };
};
