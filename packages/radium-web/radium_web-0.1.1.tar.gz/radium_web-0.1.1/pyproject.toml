[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "radium-web"
version = "0.1.1"
description = "Radium – a WSGI based Python web framework"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "Saksham Jain" }]

[project.scripts]
radium = "Radium.cli:main"

[tool.setuptools.package-data]
radium = ["templates/**"]
