# Minimal setup.py for Digital Ocean buildpack detection
# Dependencies are managed by uv via pyproject.toml
from setuptools import setup

setup(
    name="elroy",
    version="0.0.81",
    packages=["elroy"],
)
