[project]
name = "coredeco"
version = "0.5"
authors = [
    {name = "Xenely"}
]
description = "Small package with helpful and various function decorators"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
coredeco = "coredeco:main"

[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"
