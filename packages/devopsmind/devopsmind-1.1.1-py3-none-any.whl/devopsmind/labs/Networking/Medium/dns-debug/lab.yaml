id: dns-debug
title: "Debug a failed DNS lookup"
stack: networking
difficulty: Medium
xp: 100
goal: "Diagnose and identify the cause of a failed DNS lookup using provided evidence"

skills:
  - dns-troubleshooting
  - network-diagnostics
  - name-resolution

hint: "Analyze the failure message to determine why DNS resolution did not succeed."

validator: "validator.py"

mentor:
  why: "Systematic DNS troubleshooting is essential for diagnosing connectivity issues and understanding how name resolution failures occur in real-world networks."
  intent: applied

  guidance:
    before: |
      Think about the stages involved in DNS resolution and what types of failures can occur when resolvers or servers are unreachable.
    after: |
      Consider how similar DNS failure messages appear in troubleshooting tools and logs during real incidents.

  tags:
    - networking
    - dns
    - troubleshooting
    - name-resolution

execution:
  runtime: docker
  requires_execution: true
  required_tools:
    - networking
