apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: example-app

spec:
  source:
    repoURL:
    path:
    targetRevision: main

  destination:
    server:
    namespace:

  syncPolicy:
    automated:
      # TODO: define prune and selfHeal
