[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "py2gui"
version = "0.1.0"         
description = "Simple python to GUI (Tkinter) tool"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "Andy" }
]

dependencies = []
