## Three-Way Match Results

### Match Summary

{{ invoice_match_pro_three_way_matcher.match_summary | default('Match analysis is being generated...') }}

### Match Details

| Field | Value |
|-------|-------|
| **Match Status** | {{ invoice_match_pro_three_way_matcher.match_status | default('N/A') | upper }} |
| **Match Score** | {{ (invoice_match_pro_three_way_matcher.match_score * 100) | round(1) if invoice_match_pro_three_way_matcher.match_score else 'N/A' }}% |
| **PO Match** | {{ '✅ Yes' if invoice_match_pro_three_way_matcher.po_match else '❌ No' }} |
| **SOW Match** | {{ '✅ Yes' if invoice_match_pro_three_way_matcher.sow_match else '❌ No' }} |
| **Recommendation** | {{ invoice_match_pro_three_way_matcher.recommendation | default('N/A') | upper }} |

### Issues Identified

{% if invoice_match_pro_three_way_matcher.issues and invoice_match_pro_three_way_matcher.issues|length > 0 %}
{% for issue in invoice_match_pro_three_way_matcher.issues %}
- ⚠️ {{ issue }}
{% endfor %}
{% else %}
✅ No issues identified - all matches verified.
{% endif %}

---

**Review the three-way match results above. Select your decision:**
- **Approve**: Approve invoice for payment (use when match is confirmed or issues are resolved)
- **Decline**: Reject invoice (use when match fails or issues cannot be resolved)
- **Needs Clarification**: Request additional information before making a decision

