# =============================================================================
# INDIVIDUAL UI MANIFEST - Independent Agents
# =============================================================================
#
# Individual UI manifest for independent agents.
# This defines the UI behavior for independent agents that can be used outside of pipelines.
#
# Parameters:
# - title (str, required): pipeline title
# - subtitle (str, required): pipeline description
# - agents (list[object], required): list of agents
#   - agents[].id (str, required): agent identifier (must match agent id)
#   - agents[].title (str, required): human-readable agent name
#   - agents[].subtitle (str, required): agent description
#   - agents[].icon (str, required): path to agent icon image
# - interaction_diagram (str, required): path to interaction diagram SVG
#
# =============================================================================
title: "Independent Agents"
subtitle: "Available independent agents for different tasks"

agents:
  - id: "content_analyzer"
    title: "Content Analyzer"
    subtitle: "Analyzes content uploaded by user, creates summary, topics and example questions"
    icon: "assets/content_analyzer.svg"
  - id: "rag_query"
    title: "RAG Query"
    subtitle: "Query the chroma db for relevant documents based on the user's query and then perform RAG using LLM"
    icon: "assets/rag_query.svg"
  - id: "content_extractor"
    title: "Content Extractor"
    subtitle: "Extracts content from uploaded files and returns in JSON format"
    icon: "assets/content_extractor.svg"
  - id: "image_extractor"
    title: "Image Extractor"
    subtitle: "Extracts text from images and returns in JSON format"
    icon: "assets/image_extractor.svg"
  - id: "web_search"
    title: "Web Search"
    subtitle: "Search the internet for current information, news, and web-based content using search tools"
    icon: "assets/web_search.svg"

interaction_diagram: ""

