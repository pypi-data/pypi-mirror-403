// SPDX-License-Identifier: MIT OR Apache-2.0
//! GPU-accelerated stream processing (placeholder)

/// GPU processing stub
pub struct GpuProcessor;

impl GpuProcessor {
    /// Create a new GPU processor
    #[must_use]
    pub const fn new() -> Self {
        Self
    }
}

impl Default for GpuProcessor {
    fn default() -> Self {
        Self::new()
    }
}
