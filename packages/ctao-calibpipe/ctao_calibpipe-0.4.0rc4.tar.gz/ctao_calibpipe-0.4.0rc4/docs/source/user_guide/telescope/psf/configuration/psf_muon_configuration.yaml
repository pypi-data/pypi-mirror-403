CalculatePSFWithMuons:
    # Required settings
    # Input url should be pointing to an existing file.
    # It is recommended to supply it as a CLI argument.
    # input_url: "muon_0deg_0deg_run000001___cta-prod6-2156m-LaPalma-lst-dark-ref-degraded-0.8.h5"

    muonring_radius_fit_nbins: 20
    min_number_of_muon: 1
    aggregator_type: "SigmaClippingAggregator"


SigmaClippingAggregator:
    chunking_type: SizeChunking
    max_sigma: 3
    iterations: 5


MuonQualityQuery:
    quality_criteria:
      - ["min muonring radius", "muonring_radius >= 0.8"]
      - ["max muonring radius", "muonring_radius <= 1.3"]
