[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "area-jot"
version = "0.0.1"
description = "A simple Python library to calculate areas"
readme = "README.md"
authors = [{name="Jot"}]
license = {text = "MIT"}
dependencies = []