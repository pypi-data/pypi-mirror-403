[project]
name = "django-sqlite-tenants"
version = "0.1.0"
description = "Sqlite Based Multi Tenancy With Django"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.0",
]



[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
