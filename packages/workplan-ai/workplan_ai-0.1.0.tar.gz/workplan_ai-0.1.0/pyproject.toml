[project]
name = "workplan-ai"
version = "0.1.0"
description = "An AI-powered, developer-first weekly planning and execution assistant."
authors = [
    { name = "Sahil Selokar", email = "sahilselokar03@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
dependencies = [
    "typer[all]",
    "pydantic",
    "rich",
    "python-dotenv",
    "groq",
    "keyring",
]

[project.scripts]
workplan = "workplan.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
