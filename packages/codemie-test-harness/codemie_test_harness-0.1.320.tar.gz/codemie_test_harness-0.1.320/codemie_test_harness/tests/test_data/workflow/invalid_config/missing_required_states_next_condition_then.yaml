assistants:
  - id: coverage_assistant
    model: gpt-4o
    system_prompt: >
      System prompt text.

states:
  - assistant_id: coverage_assistant
    id: coverage
    task: Run
    next:
      condition:
        expression: "success == True"
        #then: end
        otherwise: end