assistants:
  - id: coverage_assistant
    model: gpt-4o
    system_prompt: >
      System prompt text.

tools:
  - id: uniqueIdentifier
    #tool: toolName

states:
  - assistant_id: coverage_assistant
    id: coverage
    task: Run
    next:
      state_id: end