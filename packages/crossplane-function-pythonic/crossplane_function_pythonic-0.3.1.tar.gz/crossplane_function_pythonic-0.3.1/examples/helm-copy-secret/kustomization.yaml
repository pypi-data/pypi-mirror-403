apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
- namespace: crossplane-system
  name: vcluster
  options:
    labels:
      function-pythonic.package: ''
  files:
  - vcluster.py

resources:
- composition.yaml
- xrd.yaml
- xr.yaml
