# SubjectTestPipeline Configuration Template
# This is a minimal configuration file for the SubjectTestPipeline
# used for testing asimov's infrastructure.

[test]
# Test pipeline configuration
pipeline = subjecttestpipeline
analysis_name = {{ production.name }}
subject_name = {{ production.subject.name }}
rundir = {{ production.rundir }}

# Number of analyses being combined
{%- if production.analyses %}
num_analyses = {{ production.analyses | size }}
{%- else %}
num_analyses = 0
{%- endif %}

# These are dummy settings for testing purposes
test_parameter_1 = 1.5
test_parameter_2 = combined_test_value
