[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "neon-static"
version = "1.0.1"
description = "Formal, static Sphinx theme with dark/light/system modes"
readme = "README.md"
license = "Apache-2.0"
authors = [
    { name = "Simone Del Popolo", email = "inbox@simonedelpopolo.com" }
]
keywords = ["sphinx", "theme", "neon-static", "documentation", "professional"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Framework :: Sphinx",
    "Framework :: Sphinx :: Theme",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Documentation :: Sphinx",
]
requires-python = ">=3.10"
dependencies = [
    "sphinx>=7.0",
]

[project.entry-points."sphinx.html_themes"]
neon-static = "neon_static"

[project.urls]
Homepage = "https://github.com/nutsloop/neon-static"
Documentation = "https://github.com/nutsloop/neon-static"

[tool.hatch.build.targets.wheel]
sources = ["src"]

[tool.hatch.build.targets.sdist]
include = [
    "src/neon_static/**",
    "README.md",
    "LICENSE",
]
