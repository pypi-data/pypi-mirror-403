[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "mls-2425-advanced-stats"
version = "0.1.0"
description = "MLS 2023-2025 Advanced Player Statistics"
requires-python = ">=3.9"
dependencies = ["pandas"]

[tool.setuptools.package-data]
mls_2425_advanced_stats = ["data/*.csv"]
