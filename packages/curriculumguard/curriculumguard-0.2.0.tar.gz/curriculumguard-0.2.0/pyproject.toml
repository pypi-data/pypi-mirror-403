[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "curriculumguard"
version = "0.2.0"
description = "Training-time adaptive data curriculum for PyTorch with safety guards"
readme = "README.md"
authors = [{name="Shaurya Jain"}]
license = {text="MIT"}
dependencies = ["torch", "numpy"]
