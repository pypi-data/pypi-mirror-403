COMMON TOOL SCENARIOS
tool return an error
 - no resources (memory/network)
 - no permissions to resource (file, network listen socket)

very long prompt
 - what if the user pastes a huge video
 - what if teh user pastes war and peace
 - what if the user pastes binary garbage

TBD: multiline prompt

JSON content
 - what if the user passes valid json to write