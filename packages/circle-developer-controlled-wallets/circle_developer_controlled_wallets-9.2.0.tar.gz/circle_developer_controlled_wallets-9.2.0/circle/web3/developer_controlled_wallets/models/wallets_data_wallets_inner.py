# coding: utf-8

"""
    The version of the OpenAPI document: 1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from circle.web3.developer_controlled_wallets.models.eoa_wallet import EOAWallet
from circle.web3.developer_controlled_wallets.models.sca_wallet import SCAWallet
from pydantic import BaseModel, ConfigDict, StrictStr, Field, ValidationError, field_validator, model_serializer
from pydantic_core import core_schema
from typing import Union, List, Set, Optional, Dict, Any
from typing_extensions import Literal, Self

WALLETSDATAWALLETSINNER_ONE_OF_SCHEMAS = ["EOAWallet", "SCAWallet"]


class WalletsDataWalletsInner(BaseModel):
    """
    WalletsDataWalletsInner
    """
    # data type: EOAWallet
    oneof_schema_1_validator: Optional[EOAWallet] = None
    # data type: SCAWallet
    oneof_schema_2_validator: Optional[SCAWallet] = None
    actual_instance: Optional[Union[EOAWallet, SCAWallet]] = None
    one_of_schemas: Set[str] = { "EOAWallet", "SCAWallet" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )



    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @classmethod
    def __get_pydantic_core_schema__(cls, source_type, handler):
        """Pydantic v2: Add automatic conversion from child types for backward compatibility"""
        
        def convert_child_types(v):
            """Convert any supported child type to OneOf wrapper automatically for backward compatibility"""
            # If it's already the correct type, return as-is
            if isinstance(v, cls):
                return v
            
            # If it's one of the child types, wrap it automatically
            if isinstance(v, EOAWallet):
                return cls(actual_instance=v)
            if isinstance(v, SCAWallet):
                return cls(actual_instance=v)
            
            # For other types, let the normal validation handle it
            return v
        
        # Get the default schema
        python_schema = handler(source_type)
        
        # Add our custom validator
        return core_schema.no_info_before_validator_function(
            convert_child_types,
            python_schema
        )

    @model_serializer
    def serialize_oneof_value(self):
        """Custom model serializer that preserves semantic types (enums, datetime objects)"""
        if self.actual_instance is None:
            return None
        
        # Return the actual_instance directly to preserve semantic types
        # This allows Pydantic to handle the serialization naturally,
        # preserving enums as enum objects and datetime as datetime objects
        return self.actual_instance

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = WalletsDataWalletsInner.model_construct()
        error_messages = []
        match = 0
        # validate data type: EOAWallet
        if not isinstance(v, EOAWallet):
            error_messages.append(f"Error! Input type `{type(v)}` is not `EOAWallet`")
        else:
            match += 1
        # validate data type: SCAWallet
        if not isinstance(v, SCAWallet):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SCAWallet`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in WalletsDataWalletsInner with oneOf schemas: EOAWallet, SCAWallet. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in WalletsDataWalletsInner with oneOf schemas: EOAWallet, SCAWallet. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        """Create an instance of WalletsDataWalletsInner from dict"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into EOAWallet
        try:
            instance.actual_instance = EOAWallet.model_validate(obj)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into SCAWallet
        try:
            instance.actual_instance = SCAWallet.model_validate(obj)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when creating `actual instance` in WalletsDataWalletsInner with oneOf schemas: EOAWallet, SCAWallet. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when creating `actual instance` in WalletsDataWalletsInner with oneOf schemas: EOAWallet, SCAWallet. Details: " + ", ".join(error_messages))
        else:
            return instance

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into EOAWallet
        try:
            instance.actual_instance = EOAWallet.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into SCAWallet
        try:
            instance.actual_instance = SCAWallet.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into WalletsDataWalletsInner with oneOf schemas: EOAWallet, SCAWallet. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into WalletsDataWalletsInner with oneOf schemas: EOAWallet, SCAWallet. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], EOAWallet, SCAWallet]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


