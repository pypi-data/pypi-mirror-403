<a
  href="${link.url}"
  tal:attributes="onclick 'false' if link.js is None else link.js; class link.css is not None and link.css"
  title="${link.title}">
  <h2>${structure:link.label}</h2>
  <p tal:condition="link.title != link.label">${link.title}</p>
</a>
