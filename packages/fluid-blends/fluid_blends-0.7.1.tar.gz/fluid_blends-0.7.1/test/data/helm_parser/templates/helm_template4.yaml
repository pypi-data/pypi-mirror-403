{{- if .Values.secret }}
  {{- if .Values.name }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.name | default "{{ .Values.name }}-access-key" }}
data:
  key: {{ .Values.key | b64enc }}
  {{- end }}