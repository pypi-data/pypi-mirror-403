apiVersion: "v1"
kind: ExternalSecret
metadata:
  name: {{ different "Values" . }}
  labels:
    app: "{{ template "Values.name" . }}"
    {{- . | nindent 4 }}
spec:
  data:
{{- range .Values.name }}
    - key: {{ $.Values.key }}
      id: {{ . }}
{{- end }}