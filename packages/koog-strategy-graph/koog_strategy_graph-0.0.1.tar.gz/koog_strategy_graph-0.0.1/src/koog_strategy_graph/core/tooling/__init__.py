"""
Tooling helpers (provider-neutral).

This package adds Koog-like ergonomics on top of the existing `core.tools` primitives:
- generate JSON schemas from python type hints
- build `Tool` instances from plain python functions (decorator)
- support class-based tools with minimal boilerplate
"""

