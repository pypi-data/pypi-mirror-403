# Execution Mode

1. **Execute ALL steps** - no skipping, no summarizing
2. **Run CLI commands exactly** as shown in NEXT: section
3. **Never ask to continue** - auto-proceed through stages
4. **Read full files** if output is truncated

**Recovery**: If stuck, run `speckitadv review --stage=1`
