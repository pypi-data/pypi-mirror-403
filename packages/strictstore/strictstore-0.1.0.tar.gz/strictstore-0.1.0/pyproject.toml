[project]
name = "strictstore"
version = "0.1.0"
description = "Strict global store with class ownership"
authors = [
  { name = "Lokesh52324" }
]
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
