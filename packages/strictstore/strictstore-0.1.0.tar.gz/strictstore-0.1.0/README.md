📦 strictstore

Strict global key-value store for Python with class ownership & write protection

strictstore is a lightweight in-memory store inspired by Redis-style access,
but with strict ownership rules to prevent accidental global mutation.