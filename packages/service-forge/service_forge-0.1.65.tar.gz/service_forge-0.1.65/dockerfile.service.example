FROM crpi-epzyo5y512mtnalv.cn-hangzhou.personal.cr.aliyuncs.com/second_brain/service_forge:v1.0.0

WORKDIR /app

COPY . ./service

WORKDIR /app
RUN uv sync

ENV PYTHONPATH=/app/service:/app:/app/src
ENV PATH="/app/.venv/bin:$PATH"

WORKDIR /app/service

CMD ["uv", "run", "python", "-m", "main"]
