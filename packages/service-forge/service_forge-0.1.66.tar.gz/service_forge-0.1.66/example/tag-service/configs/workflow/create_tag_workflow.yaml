name: create_tag_workflow
description: Create a new tag
nodes:
  - name: fast_api_server
    type: FastAPITrigger
    args:
      app: app
      path: /tag
      method: POST
      data_type: SfBaseModel.CreateTagModel
    outputs:
      data: http_create_tag|tag
      user_id: http_create_tag|user_id

  - name: http_create_tag
    type: HttpCreateTagNode
    args:
    outputs: 

outputs:
  - name: result
    port: http_create_tag|tag