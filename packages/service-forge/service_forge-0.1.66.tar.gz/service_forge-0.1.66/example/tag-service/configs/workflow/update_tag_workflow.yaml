name: update_tag_workflow
description: Update a tag
nodes:
  - name: fast_api_server
    type: FastAPITrigger
    args:
      app: app
      path: /tag
      method: PUT
      data_type: SfBaseModel.UpdateTagModel
    outputs:
      data: http_update_tag|tag
      user_id: http_update_tag|user_id

  - name: http_update_tag
    type: HttpUpdateTagNode
    args:
    outputs: 

outputs:
  - name: result
    port: http_update_tag|tag