Use `uv` for package management. 
Use `just` for command running, see the `Justfile`
Follow strict TDD unless otherwise instructed. Create a stub impl alongside tests, and then in a later commit create the impl. Ask your partner when you're ready to proceed to the impl.
