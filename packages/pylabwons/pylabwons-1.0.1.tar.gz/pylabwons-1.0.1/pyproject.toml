[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pylabwons"
version = "1.0.1"
description = "SNOWBALL REVERSES LABWONS"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "plotly",
    "pyarrow",
    "pykrx",
    "numpy",
    "scipy",
    "ta"
]