[build]
rustflags = [
    "-C", "target-cpu=native", # Gain access to CPU popcnt and ctzl (recommended by chess crate)
]

[target.'cfg(target_os = "linux")']
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
