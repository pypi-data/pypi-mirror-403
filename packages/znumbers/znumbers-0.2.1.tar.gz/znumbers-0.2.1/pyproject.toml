[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "znumbers"
version = "0.2.1"
description = "Z-numbers arithmetic library (Lotfi Zadeh’s Z-numbers, with 4 basic operations)"
authors = [
  { name = "Dilmurod Yusupov Tasbaltayevich", email = "cazinoroyal1987@gmail.com" }
]
readme = "README.md"
license = { file = "LICENSE" }
dependencies = [
  "numpy",
  "scipy"
]

[tool.setuptools]
packages = ["znumbers"]
