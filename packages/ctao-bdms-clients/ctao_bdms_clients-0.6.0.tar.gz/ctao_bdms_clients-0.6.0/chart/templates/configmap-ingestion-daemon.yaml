---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "bdms.fullname" . }}-ingestion-daemon-config
data:
  config.yaml: |
    # Configuration for the ingestion daemon

    {{ .Values.acada_ingest.daemon.config | toYaml | nindent 4 }}
