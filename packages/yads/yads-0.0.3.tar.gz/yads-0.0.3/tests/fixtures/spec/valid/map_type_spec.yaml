name: "catalog.db.map_spec"
version: 1
yads_spec_version: "0.0.2"
columns:
  - name: "metadata"
    type: "map"
    params:
      keys_sorted: true
    key:
      type: "string"
      params:
        length: 50
    value:
      type: "integer" 