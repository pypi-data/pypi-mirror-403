[tool:pytest]
asyncio_mode = auto
testpaths = tests/unit tests/integration
addopts = --cov=metapg.pool --cov-report=term-missing -v
markers =
    unit: Unit tests that do not require database connection
    integration: Integration tests that require database connection
    slow: Tests that are slow to run