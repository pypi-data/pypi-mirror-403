{
  "settings": {
    "customInstructions": "For TypeScript/JavaScript projects, use bun as the package manager and runtime.\nUse `bun install` for dependencies, `bun run` for scripts, `bun test` for testing.\nFor Python projects, use uv as the package manager.\nUse `uv pip install` for dependencies, `uv run` for running scripts, `uv venv` for virtual environments."
  }
}