"""persistence.domain

Pure Studio domain rules and helpers (no transport concerns).

Examples:
- invariants (name uniqueness, parent validation, cycle prevention)
- tree projection (workspace filesystem tree)
- patch semantics helpers (PathwayDoc normalization/validation)
"""
