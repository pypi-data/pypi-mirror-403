[project]
name = "cvm-dify"
version = "0.1.2"
description = "CVM Dify MCP Server "
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.26.0",
    "requests>=2.28.0",
]

[project.scripts]
cvm-dify = "cvm_dify:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
