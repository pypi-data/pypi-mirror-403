[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ascii-builder-z"
version = "0.1.0"
authors = [
    { name = "3xboiz", email = "author@example.com" },
]
description = "A thick ASCII art generator"
readme = "main.md"
requires-python = ">=3.7"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["az*"]
