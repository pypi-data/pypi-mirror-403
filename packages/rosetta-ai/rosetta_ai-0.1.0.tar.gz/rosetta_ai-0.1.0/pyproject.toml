[project]
name = "rosetta-ai"
version = "0.1.0"
description = "The translation layer for LLM provider messages"
authors = [{ name = "Latitude Data SL", email = "hello@latitude.so" }]
maintainers = [{ name = "Latitude Data SL", email = "hello@latitude.so" }]
readme = "README.md"
license = "MIT"
urls.repository = "https://github.com/latitude-dev/rosetta-py"
urls.homepage = "https://github.com/latitude-dev/rosetta-py#readme"
urls.documentation = "https://github.com/latitude-dev/rosetta-py#readme"
requires-python = ">=3.9, <3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
