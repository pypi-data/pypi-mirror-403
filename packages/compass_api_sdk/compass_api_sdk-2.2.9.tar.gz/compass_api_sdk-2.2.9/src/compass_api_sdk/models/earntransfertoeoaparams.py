"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from compass_api_sdk.types import BaseModel, UNSET_SENTINEL
from compass_api_sdk.utils import validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Any, Literal, Optional
from typing_extensions import Annotated, TypedDict


class EarnTransferToEOAParamsTypedDict(TypedDict):
    r"""Parameters for transferring tokens from Earn Account to EOA (withdrawal).

    Used by multicall/bundle endpoint to send tokens from the Earn Account (Safe) back
    to the owner's EOA as part of an atomic bundle transaction.

    No signature is required for this action since the Safe already owns the tokens and
    is executing the transfer as part of its own transaction.
    """

    token: str
    r"""The token to transfer from Earn Account to EOA."""
    amount: Any
    r"""The amount of tokens to transfer (in token units, not wei), or 'ALL' to transfer the entire balance."""
    action_type: Literal["V2_TRANSFER_TO_EOA"]
    r"""Action type identifier for Earn-Account-to-EOA transfer operations."""


class EarnTransferToEOAParams(BaseModel):
    r"""Parameters for transferring tokens from Earn Account to EOA (withdrawal).

    Used by multicall/bundle endpoint to send tokens from the Earn Account (Safe) back
    to the owner's EOA as part of an atomic bundle transaction.

    No signature is required for this action since the Safe already owns the tokens and
    is executing the transfer as part of its own transaction.
    """

    token: str
    r"""The token to transfer from Earn Account to EOA."""

    amount: Any
    r"""The amount of tokens to transfer (in token units, not wei), or 'ALL' to transfer the entire balance."""

    ACTION_TYPE: Annotated[
        Annotated[
            Optional[Literal["V2_TRANSFER_TO_EOA"]],
            AfterValidator(validate_const("V2_TRANSFER_TO_EOA")),
        ],
        pydantic.Field(alias="action_type"),
    ] = "V2_TRANSFER_TO_EOA"
    r"""Action type identifier for Earn-Account-to-EOA transfer operations."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["action_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
