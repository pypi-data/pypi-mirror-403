"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from compass_api_sdk.types import BaseModel, UNSET_SENTINEL
from compass_api_sdk.utils import validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import Literal, Optional, Union
from typing_extensions import Annotated, TypeAliasType, TypedDict


EarnTransferFromEOAParamsAmountTypedDict = TypeAliasType(
    "EarnTransferFromEOAParamsAmountTypedDict", Union[float, str]
)
r"""The amount of tokens to transfer (in token units, not wei)."""


EarnTransferFromEOAParamsAmount = TypeAliasType(
    "EarnTransferFromEOAParamsAmount", Union[float, str]
)
r"""The amount of tokens to transfer (in token units, not wei)."""


class EarnTransferFromEOAParamsTypedDict(TypedDict):
    r"""Parameters for transferring tokens from EOA to Earn Account via Permit2.

    Used by multicall/bundle endpoint to pull tokens from the owner's EOA
    into their Earn Account (Safe) as part of an atomic bundle transaction.

    The owner must have:
    1. Previously approved Permit2 for the token (one-time setup via /gas_sponsorship/approve_transfer)
    2. Signed a Permit2 message with spender = Earn Account address

    The signature is obtained by first calling /v2/earn/transfer with action=DEPOSIT
    and gas_sponsorship=True, then signing the returned EIP-712 typed data.
    """

    token: str
    r"""The token to transfer from EOA to Earn Account."""
    amount: EarnTransferFromEOAParamsAmountTypedDict
    r"""The amount of tokens to transfer (in token units, not wei)."""
    permit2_signature: str
    r"""The EOA owner's signature of the Permit2 PermitTransferFrom typed data. Obtain this by calling /v2/earn/transfer with action=DEPOSIT and gas_sponsorship=True, then signing the returned EIP-712 data with the owner's wallet."""
    permit2_nonce: int
    r"""The nonce used in the Permit2 signature (from the signed typed data)."""
    permit2_deadline: int
    r"""The deadline timestamp used in the Permit2 signature (from the signed typed data)."""
    action_type: Literal["V2_TRANSFER_FROM_EOA"]
    r"""Action type identifier for EOA-to-Earn-Account transfer operations."""


class EarnTransferFromEOAParams(BaseModel):
    r"""Parameters for transferring tokens from EOA to Earn Account via Permit2.

    Used by multicall/bundle endpoint to pull tokens from the owner's EOA
    into their Earn Account (Safe) as part of an atomic bundle transaction.

    The owner must have:
    1. Previously approved Permit2 for the token (one-time setup via /gas_sponsorship/approve_transfer)
    2. Signed a Permit2 message with spender = Earn Account address

    The signature is obtained by first calling /v2/earn/transfer with action=DEPOSIT
    and gas_sponsorship=True, then signing the returned EIP-712 typed data.
    """

    token: str
    r"""The token to transfer from EOA to Earn Account."""

    amount: EarnTransferFromEOAParamsAmount
    r"""The amount of tokens to transfer (in token units, not wei)."""

    permit2_signature: str
    r"""The EOA owner's signature of the Permit2 PermitTransferFrom typed data. Obtain this by calling /v2/earn/transfer with action=DEPOSIT and gas_sponsorship=True, then signing the returned EIP-712 data with the owner's wallet."""

    permit2_nonce: int
    r"""The nonce used in the Permit2 signature (from the signed typed data)."""

    permit2_deadline: int
    r"""The deadline timestamp used in the Permit2 signature (from the signed typed data)."""

    ACTION_TYPE: Annotated[
        Annotated[
            Optional[Literal["V2_TRANSFER_FROM_EOA"]],
            AfterValidator(validate_const("V2_TRANSFER_FROM_EOA")),
        ],
        pydantic.Field(alias="action_type"),
    ] = "V2_TRANSFER_FROM_EOA"
    r"""Action type identifier for EOA-to-Earn-Account transfer operations."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["action_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
