[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vallignus"
version = "0.3.0"
description = "Infrastructure-grade firewall for AI agents with identity, policy versioning, and audit"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  {name = "Jacob Gadek", email = "your.email@example.com"}
]
keywords = ["ai", "security", "firewall", "agent", "llm", "authority"]
dependencies = [
    "browser-cookie3",
    "click",
    "mitmproxy>=9.0.0,<10.0.0",
    "rich",
    "cryptography>=38.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

[project.urls]
Repository = "https://github.com/jacobgadek/vallignus"

[project.scripts]
vallignus = "vallignus.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]