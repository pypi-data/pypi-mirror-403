# src/meson.build - 主程序构建配置

py_sources = files('main.py')

executable(
    '{{ project_name }}',
    code_gen.process(py_sources),
    include_directories: inc_dir,
    dependencies: [mclang_dep],
    install: true,
)
