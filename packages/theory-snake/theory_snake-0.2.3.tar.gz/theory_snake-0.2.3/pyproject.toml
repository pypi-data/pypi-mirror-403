[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "theory_snake"  # Change this if you pick a different name
version = "0.2.3"  # Updated from 0.2.2
authors = [
  { name="Omkar Pradhan", email="omkarelectric892@gmail.com" },
]
description = "A simple Python library for generating music chords and scales."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Topic :: Multimedia :: Sound/Audio :: Analysis",
]
dependencies = []

