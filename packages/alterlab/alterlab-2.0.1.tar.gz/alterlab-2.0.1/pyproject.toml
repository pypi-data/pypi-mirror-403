[tool.poetry]
name = "alterlab"
version = "2.0.1"
description = "AlterLab Python SDK"
authors = ["AlterLab <dev@alterlab.io>"]

[tool.poetry.dependencies]
python = "^3.8"
httpx = "^0.25.2"
pydantic = "^2.5.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.3"
pytest-asyncio = "^0.21.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
