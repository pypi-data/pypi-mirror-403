[project]
name = "pulse-mantine"
version = "0.1.29"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Erwin Kuhn", email = "erwin.kuhn@protonmail.com" }
]
requires-python = ">=3.12"
dependencies = ["pulse-framework>=0.1.18"]

[tool.uv]
package = true

[build-system]
requires = ["uv_build>=0.8.13,<0.9.0"]
build-backend = "uv_build"
