[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.ruff]
exclude = [
    "shaped/autogen",
    "shaped/__init__.py",
]

[tool.ruff.lint]
exclude = [
    "shaped/autogen",
    "shaped/__init__.py",
]
