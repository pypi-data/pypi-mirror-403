[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "zyt-si"
version = "2.0.2"
description = "Git-based package manager CLI"
requires-python = ">=3.8"
readme = "README.md"
license = { text = "GNU General Public License v3" }

authors = [
{ name = "Samail Islam" }
]

[project.scripts]
zyt = "zyt.cli:main"
