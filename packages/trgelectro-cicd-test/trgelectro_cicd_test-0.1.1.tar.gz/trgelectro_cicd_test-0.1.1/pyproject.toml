[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trgelectro-cicd-test"
version = "0.1.1"
description = "A minimal hello world Python application"
authors = [{name = "trgelectro", email = "trgelectro@email.com"}]