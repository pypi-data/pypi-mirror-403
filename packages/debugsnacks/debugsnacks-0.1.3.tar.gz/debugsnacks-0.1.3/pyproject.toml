[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "debugsnacks"
version = "0.1.3"
description = "A playful Python utility library for fun, facts, and chaos"
authors = [{name = "Mohammad Razi"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/yourusername/debugsnacks"
