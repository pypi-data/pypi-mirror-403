Adding a remote controller to a local controller:

    - On the local controller, select Add Controller in settings
    - Copy the accessKey
    - On the remote controller,select Register Remote Access Key in settings
    - Paste the main controller Ip and access Key





