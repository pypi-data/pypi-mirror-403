# Environment variables for docker-compose and local dev
# Copy this file to .env (kept untracked by .gitignore) and adjust values.

# ArangoDB root password used by the Docker container
ARANGO_ROOT_PASSWORD=changeme

# Optional: standard app variables (used by README/quickstarts)
ARANGO_URL=http://localhost:8529
ARANGO_DB=mcp_arangodb_test
ARANGO_USERNAME=mcp_arangodb_user
ARANGO_PASSWORD=mcp_arangodb_password

# Optional: new password for user self-service password change
# Used by: mcp-arangodb-async user password --env-file .env
ARANGO_NEW_PASSWORD=

# Optional: tuning
ARANGO_CONNECT_RETRIES=10
ARANGO_CONNECT_DELAY_SEC=1.0
ARANGO_TIMEOUT_SEC=30.0
LOG_LEVEL=INFO

# ============================================================================
# MCP Transport Configuration (Phase 2)
# ============================================================================

# Transport Selection
# Options: stdio (default, for desktop clients) or http (for web/containerized deployments)
MCP_TRANSPORT=stdio

# HTTP-Specific Configuration (only used when MCP_TRANSPORT=http)
# Host address to bind to (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
MCP_HTTP_HOST=0.0.0.0

# Port number for HTTP server
MCP_HTTP_PORT=8000

# Stateless mode (true/false)
# - false (default): Stateful mode, maintains session state (best for single-instance)
# - true: Stateless mode, no session state (required for horizontal scaling)
MCP_HTTP_STATELESS=false

# CORS origins (comma-separated list)
# - * (default): Allow all origins (convenient for development)
# - Production: Restrict to specific origins, e.g., https://app.example.com,https://admin.example.com
MCP_HTTP_CORS_ORIGINS=*
