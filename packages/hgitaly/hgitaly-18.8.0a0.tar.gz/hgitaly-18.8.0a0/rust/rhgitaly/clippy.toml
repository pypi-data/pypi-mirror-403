# MSRV is the max of
# - Mercurial's, defined in its own clippy.toml, and meant to be no higher than
#   the version in Debian testing
# - the Rust version in Heptapod CI image (itself inherited from GitLab CI
#   images
msrv = "1.85.1"
