[project]
name = "hexa-sdk"
version = "0.1.0"
description = "A short description of your project"
authors = [
    {name = "Felix", email = "lampkole@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    # Poetry still manages these, but the standard 
    # allows them here too if you aren't using tool.poetry.dependencies
]

[tool.poetry]
# Keep Poetry-specific settings here, like:
packages = [{include = "hexa", from = "src"}]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.31.0"

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"