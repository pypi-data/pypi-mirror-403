[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bm-group"
version = "0.1.1"
description = "CLI tool to group SMILES by Bemis–Murcko scaffold, with deterministic 3D conformer generation and on-disk caching (RDKit-based)."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = [
  "tqdm>=4.0",
  "rdkit>=2024.03.5"
]

[project.scripts]
bm-group = "bm_group:main"

[tool.setuptools]
py-modules = ["bm_group"]
include-package-data = false
