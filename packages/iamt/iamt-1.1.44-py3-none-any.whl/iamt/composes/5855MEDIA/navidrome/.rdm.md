
https://github.com/navidrome/navidrome
https://www.navidrome.org/docs/installation/docker/



# 折腾了半天终于成功了
# 如果不更改 官方的 默认 compose配置  容器内会使用 1000 用户启动项目 
# 一定要事先用1000用户 本地创建好 一个data 文件夹用来映射容器内 /data 文件夹才可以 
# 我直接在项目目录 创建了data目录 用vagrant用户复制到 目标主机上 测试成功了 

# 如果不自己手动创建这个data文件夹 新建的文件夹默认UG是 root:root 

# 或者直接注释掉官方的 user: 1000:1000 
# 然后 容器内会 直接使用 root 运行程序 容器外也用root 就行了 
