
services:
    new-api:
        container_name: new-api
        restart: always
        ports:
            - ${PORT:-3000}:3000
        environment:
            - TZ=Asia/Shanghai
        volumes:
            - ./new-api:/data
        image: "${REGISTRY:-docker.io}/calciumion/new-api:${VERSION:-latest}"



