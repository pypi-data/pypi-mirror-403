https://docs.gitlab.com/install/docker/installation/#install-gitlab-by-using-docker-compose
https://hub.docker.com/r/gitlab/gitlab-ee
https://gitlab.com/gitlab-org/gitlab
https://hub.docker.com/r/gitlab/gitlab-ce

# gitlab 默认密码在 容器当中  /etc/gitlab/initxxx 文件中
# 默认用户名是root
sudo cat /opt/docker/gitlab_V1_0/config/initial_root_password


# 本来想看一下 能不能直接在启动的时候设置root密码的最后发现完全不行
# GITLAB_ROOT_PASSWORD: 'kingSUNG509!'
# gitlab_rails['initial_root_password']= 'kingSUNG509!'
# 这两个设置都没用 最后只能在UI哪里设置了



# 目前 gitlab的部署还真的存在不小的问题 
# 域名配合自签名证书用不了  现在测试的结果是 IP配合自签名证书可以用的 
# 至于 SSH方式 从来就没有成功过  2222一直拒绝连接  
