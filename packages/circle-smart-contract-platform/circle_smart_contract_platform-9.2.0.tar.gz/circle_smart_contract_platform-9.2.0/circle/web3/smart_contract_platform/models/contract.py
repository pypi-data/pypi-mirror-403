# coding: utf-8

"""
    The version of the OpenAPI document: 1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json
import uuid
from uuid import UUID
from pydantic import StrictStr, ValidationError, JsonValue
from enum import Enum
from datetime import datetime

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from circle.web3.smart_contract_platform.models.blockchain import Blockchain
from circle.web3.smart_contract_platform.models.contract_input_type import ContractInputType
from circle.web3.smart_contract_platform.models.contract_status import ContractStatus
from circle.web3.smart_contract_platform.models.event import Event
from circle.web3.smart_contract_platform.models.function import Function
from circle.web3.smart_contract_platform.models.sol_file import SolFile
from circle.web3.smart_contract_platform.models.verification_status import VerificationStatus
from pydantic import StrictStr  # Additional import for UUID backward compatibility
from typing import Optional, Set
from typing_extensions import Self

class Contract(BaseModel):
    """
    Contract
    """ # noqa: E501
    id: StrictStr = Field(description="System-generated unique identifier of the resource.", alias="id")
    deployer_wallet_id: Optional[StrictStr] = Field(None, description="The &#x60;id&#x60; of the Circle Wallet that deployed this contract.", alias="deployerWalletID")
    deployment_transaction_id: Optional[StrictStr] = Field(None, description="The id of the deployment transaction for this contract.", alias="deploymentTransactionId")
    tx_hash: Optional[StrictStr] = Field(default=None, description="Blockchain generated identifier of the transaction.", alias="txHash")
    abi_json: Optional[StrictStr] = Field(default=None, description="The contract's ABI in a JSON stringified format.", alias="abiJson")
    archived: StrictBool = Field(description="The archive state of the contract. If true, the contract will not be visible in your dashboard.")
    blockchain: Blockchain
    bytecode: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, description="Bytecode of the contract being deployed.")
    contract_address: Optional[StrictStr] = Field(default=None, description="The on-chain address of this contract.", alias="contractAddress")
    contract_input_type: ContractInputType = Field(alias="contractInputType")
    deployer_address: Optional[StrictStr] = Field(default=None, description="The address that created this contract, if deployed.", alias="deployerAddress")
    deployer_user_id: Optional[StrictStr] = Field(None, description="", alias="deployerUserID")
    deployment_error_reason: Optional[StrictStr] = Field(default=None, alias="deploymentErrorReason")
    deployment_error_details: Optional[StrictStr] = Field(default=None, alias="deploymentErrorDetails")
    name: StrictStr = Field(description="The name for a contract. Must be alphanumeric [a-zA-Z0-9].")
    description: Optional[StrictStr] = Field(default=None, description="The description for a contract.")
    status: ContractStatus
    verification_status: VerificationStatus = Field(alias="verificationStatus")
    metadata_link: Optional[StrictStr] = Field(default=None, description="The ipfs metadata link of the contract.", alias="metadataLink")
    update_date: datetime = Field(description="Date and time the resource was last updated, in ISO-8601 UTC format.", alias="updateDate")
    create_date: datetime = Field(description="Date and time the resource was created, in ISO-8601 UTC format.", alias="createDate")
    source_code: List[SolFile] = Field(alias="sourceCode")
    functions: List[Function] = Field(description="Functions supported by this contract. Parsed from abi_json.")
    events: List[Event] = Field(description="Events this contract can emit. Parsed from abi_json.")
    implementation_contract: Optional[Dict[str, Any]] = Field(default=None, description="Object of the implementation contract.", alias="implementationContract")
    __properties: ClassVar[List[str]] = ["id", "deployerWalletID", "deploymentTransactionId", "txHash", "abiJson", "archived", "blockchain", "bytecode", "contractAddress", "contractInputType", "deployerAddress", "deployerUserID", "deploymentErrorReason", "deploymentErrorDetails", "name", "description", "status", "verificationStatus", "metadataLink", "updateDate", "createDate", "sourceCode", "functions", "events", "implementationContract"]

    def __init__(self, **kwargs):
        if "idempotencyKey" in self.__properties and not kwargs.get("idempotency_key"):
            kwargs["idempotency_key"] = "#REFILL_PLACEHOLDER"

        if "entitySecretCiphertext" in self.__properties and not kwargs.get("entity_secret_ciphertext"):
            kwargs["entity_secret_ciphertext"] = "#REFILL_PLACEHOLDER"
        super().__init__(**kwargs)

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        excluded_fields: Set[str] = set([
            "source_code",
        ])
        return self.model_dump_json(
            by_alias=True, 
            exclude=excluded_fields,
            exclude_none=True  # Backwards compatible: exclude None values like Pydantic v1
        )

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Contract from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "source_code",
        ])

        
        # Step 1: Get Python-native dict (datetime objects preserved)
        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
            mode='python'  # Preserves datetime objects as-is
        )
        
        # Step 2: Normalize only UUID and Enum objects to strings for v7.0.0 compatibility
        def normalize_for_legacy(obj):
            """Convert UUID and Enum objects to strings while preserving datetime objects"""
            
            if isinstance(obj, Enum):
                return obj.value
            elif isinstance(obj, UUID):
                return str(obj)
            elif isinstance(obj, list):
                return [normalize_for_legacy(item) for item in obj]  # Recursively handle arrays
            elif isinstance(obj, dict):
                return {k: normalize_for_legacy(v) for k, v in obj.items()}  # Recursively handle dicts
            else:
                return obj  # datetime objects and other types preserved as-is
        
        _dict = normalize_for_legacy(_dict)
        # override the default output from pydantic by calling `to_dict()` of each item in source_code (list)
        _items = []
        if self.source_code:
            for _item_source_code in self.source_code:
                if _item_source_code:
                    _items.append(_item_source_code.to_dict())
            _dict['sourceCode'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in functions (list)
        _items = []
        if self.functions:
            for _item_functions in self.functions:
                if _item_functions:
                    _items.append(_item_functions.to_dict())
            _dict['functions'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in events (list)
        _items = []
        if self.events:
            for _item_events in self.events:
                if _item_events:
                    _items.append(_item_events.to_dict())
            _dict['events'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Contract from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        
        
        # Process input data for backward compatibility
        # Use obj directly to reduce template modifications

        # Circle: Pre-validate strict string fields only (targeted approach)
        # Dynamically detect all StrictStr fields from model definition        
        # Only validate Circle-specific fields for backward compatibility
        # UUID fields are now handled by conversion in __init__, so no dynamic detection needed
        strict_str_fields = ["idempotencyKey", "entitySecretCiphertext"]

        for key, value in obj.items():
            if value is not None and key in strict_str_fields and not isinstance(value, str):
                # Create a simple validation error message for Pydantic v2
                raise ValidationError.from_exception_data("ValidationError", [
                    {
                        "type": "string_type",
                        "loc": (key,),
                        "msg": f"Input should be a valid string for field '{key}', got {type(value).__name__}: {value}",
                        "input": value,
                    }
                ])
        
        # Keep UUID field 'id' as string for backward compatibility
        # Only convert UUID objects to strings, leave other types for Pydantic validation
        if obj.get("id") is not None:
            uuid_value = obj["id"]
            if isinstance(uuid_value, uuid.UUID):
                obj["id"] = str(uuid_value)
        # Keep UUID field 'deployerWalletID' as string for backward compatibility
        # Only convert UUID objects to strings, leave other types for Pydantic validation
        if obj.get("deployerWalletID") is not None:
            uuid_value = obj["deployerWalletID"]
            if isinstance(uuid_value, uuid.UUID):
                obj["deployerWalletID"] = str(uuid_value)
        # Keep UUID field 'deploymentTransactionId' as string for backward compatibility
        # Only convert UUID objects to strings, leave other types for Pydantic validation
        if obj.get("deploymentTransactionId") is not None:
            uuid_value = obj["deploymentTransactionId"]
            if isinstance(uuid_value, uuid.UUID):
                obj["deploymentTransactionId"] = str(uuid_value)
        # Keep UUID field 'deployerUserID' as string for backward compatibility
        # Only convert UUID objects to strings, leave other types for Pydantic validation
        if obj.get("deployerUserID") is not None:
            uuid_value = obj["deployerUserID"]
            if isinstance(uuid_value, uuid.UUID):
                obj["deployerUserID"] = str(uuid_value)
        
        # Circle auto-fill logic for missing/null values (matches __init__ behavior)
        # Handle idempotencyKey auto-fill
        if "idempotencyKey" in cls.__properties:
            if obj.get("idempotencyKey") is None:
                obj["idempotencyKey"] = "#REFILL_PLACEHOLDER"
        
        # Handle entitySecretCiphertext auto-fill  
        if "entitySecretCiphertext" in cls.__properties:
            if obj.get("entitySecretCiphertext") is None:
                obj["entitySecretCiphertext"] = "#REFILL_PLACEHOLDER"
        
        _obj = cls.model_validate({
            "id": obj.get("id"),
            "deployerWalletID": obj.get("deployerWalletID"),
            "deploymentTransactionId": obj.get("deploymentTransactionId"),
            "txHash": obj.get("txHash"),
            "abiJson": obj.get("abiJson"),
            "archived": obj.get("archived"),
            "blockchain": obj.get("blockchain"),
            "bytecode": obj.get("bytecode"),
            "contractAddress": obj.get("contractAddress"),
            "contractInputType": obj.get("contractInputType"),
            "deployerAddress": obj.get("deployerAddress"),
            "deployerUserID": obj.get("deployerUserID"),
            "deploymentErrorReason": obj.get("deploymentErrorReason"),
            "deploymentErrorDetails": obj.get("deploymentErrorDetails"),
            "name": obj.get("name"),
            "description": obj.get("description"),
            "status": obj.get("status"),
            "verificationStatus": obj.get("verificationStatus"),
            "metadataLink": obj.get("metadataLink"),
            "updateDate": obj.get("updateDate"),
            "createDate": obj.get("createDate"),
            "sourceCode": [SolFile.from_dict(_item) for _item in obj["sourceCode"]] if obj.get("sourceCode") is not None else None,
            "functions": [Function.from_dict(_item) for _item in obj["functions"]] if obj.get("functions") is not None else None,
            "events": [Event.from_dict(_item) for _item in obj["events"]] if obj.get("events") is not None else None,
            "implementationContract": obj.get("implementationContract")
        })
        return _obj


