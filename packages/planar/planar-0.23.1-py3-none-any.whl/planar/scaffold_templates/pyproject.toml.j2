[project]
name = "{{ name }}"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
{%- if local_source %}
    "planar",
{%- else %}
    "planar>=0.23.0",
{%- endif %}
]

{%- if local_source %}

[tool.uv.sources]
planar = { path = "{{ planar_source_path }}", editable = true }

{%- endif %}
