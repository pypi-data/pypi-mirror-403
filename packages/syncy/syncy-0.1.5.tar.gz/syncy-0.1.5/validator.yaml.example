source:
  url: ${SYNCY_SOURCE_URL}
target:
  url: ${SYNCY_TARGET_URL}
include_schemas: [public]
exclude_schemas: []
validate: [tables, views, procedures, functions, triggers]
rules: rules.yaml.example  # or inline list of rules
