[project]
name = "rclco"
version = "0.1.2"
description = "RCLCO Python library"
authors = [
    {name = "scott-stoltzman-consulting", email = "scott@stoltzmanconsulting.com"}
]
requires-python = ">=3.10"
readme = "README.md"

[project.optional-dependencies]
dev = [
    "pytest>=5.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
