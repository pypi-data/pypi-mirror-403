[project]
name = "sdata-client"
version = "0.1.0"
description = "Python client for sdata FastAPI service."
authors = [
    { name = "Danny Zhang", email = "zhangyi_ZJ@foxmail.com" }
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.25.0"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
