[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "websuper"
version = "1.0.0"
description = "Ngrok-like tunneling tool"
dependencies = ["requests", "flask"]

[project.scripts]
websuper = "websuper.cli:main"
