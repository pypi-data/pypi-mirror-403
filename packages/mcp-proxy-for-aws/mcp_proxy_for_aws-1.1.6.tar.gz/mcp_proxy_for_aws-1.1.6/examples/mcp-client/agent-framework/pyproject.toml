[project]
name = "mcp-client-example-agent-framework"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "agent-framework",
    "mcp-proxy-for-aws",
]

[tool.uv]
prerelease = "allow"

[tool.uv.sources]
mcp-proxy-for-aws = { workspace = true }
