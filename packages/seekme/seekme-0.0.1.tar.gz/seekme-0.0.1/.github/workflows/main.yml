name: Main

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}

      - name: Set up the environment
        uses: ./.github/actions/setup-python-env

      - name: Run checks
        run: make check

  tests-and-type-check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - python-version: "3.10"
            local-embeddings: "0"
          - python-version: "3.11"
            local-embeddings: "1"
          - python-version: "3.12"
            local-embeddings: "0"
          - python-version: "3.13"
            local-embeddings: "0"
          - python-version: "3.14"
            local-embeddings: "0"
      fail-fast: false
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up the environment
        uses: ./.github/actions/setup-python-env
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install test dependencies
        run: |
          extras="--extra mysql --extra remote-embeddings"
          if [ "${{ matrix.local-embeddings }}" = "1" ]; then
            extras="$extras --extra local-embeddings"
          fi
          uv sync --frozen $extras --group dev

      - name: Prepare test environment
        run: |
          cat <<EOF > .env
          SEEKME_TEST_DB_MODE=remote
          SEEKME_TEST_DB_HOST=127.0.0.1
          SEEKME_TEST_DB_PORT=2881
          SEEKME_TEST_DB_USER=root
          SEEKME_TEST_DB_PASSWORD=
          SEEKME_TEST_DB_NAME=seekme_test
          SEEKME_TEST_LOCAL_EMBEDDING=${{ matrix.local-embeddings }}
          EOF

      - name: Free disk space
        uses: kfir4444/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          swap-storage: true

      - name: Start seekdb
        run: |
          docker run --name seekdb-server -p 2881:2881 -d oceanbase/seekdb:1.0.0.0-100000262025111218
          sleep 15
          docker logs seekdb-server

      - name: Wait for seekdb
        run: |
          uv run python - <<'PY'
          import time
          import pymysql

          deadline = time.time() + 60
          last_error = None
          while time.time() < deadline:
            try:
              conn = pymysql.connect(host="127.0.0.1", port=2881, user="root", password="", connect_timeout=2)
              with conn.cursor() as cur:
                cur.execute("SELECT 1")
                cur.fetchone()
              conn.close()
              print("seekdb ready")
              break
            except Exception as exc:
              last_error = exc
              time.sleep(2)
          else:
            raise SystemExit(f"seekdb not ready: {last_error}")
          PY

      - name: Run tests
        run: uv run python -m pytest -m "not e2e" tests

      - name: Run e2e tests
        if: matrix.python-version == '3.11'
        env:
          SEEKME_TEST_REMOTE_API_KEY: ${{ secrets.DASHSCOPE_API_KEY }}
        run: |
          if [ -z "$SEEKME_TEST_REMOTE_API_KEY" ]; then
            echo "DASHSCOPE_API_KEY is not set; skipping e2e embedding tests."
            exit 0
          fi
          cat <<EOF >> .env
          SEEKME_TEST_REMOTE_EMBEDDING=1
          SEEKME_TEST_REMOTE_API_KEY=$SEEKME_TEST_REMOTE_API_KEY
          EOF
          uv run python -m pytest -m e2e tests/e2e

      - name: Stop seekdb
        if: always()
        run: docker rm -f seekdb-server

      - name: Check typing
        run: uv run ty check



  embedded-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up the environment
        uses: ./.github/actions/setup-python-env
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install test dependencies
        run: uv sync --frozen --extra seekdb --group dev

      - name: Run embedded tests
        env:
          SEEKME_TEST_DB_MODE: embedded
          SEEKME_TEST_SEEKDB_PATH: /tmp/seekdb.db
          SEEKME_TEST_LOCAL_EMBEDDING: "0"
        run: make test


  check-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up the environment
        uses: ./.github/actions/setup-python-env

      - name: Check if documentation can be built
        run: uv run mkdocs build -s
