[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sko-reborn"
version = "0.1.4"
description = "Random person generator (fictional data only)"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  {name = "Eren"}
]

[project.urls]
Homepage = "https://pynet.neocities.org"

