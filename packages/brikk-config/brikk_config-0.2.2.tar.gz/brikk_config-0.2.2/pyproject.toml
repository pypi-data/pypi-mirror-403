[project]
name = "brikk-config"
version = "0.2.2"
description = "brikk-config"
authors = [
    { name = "Lucino772" }
]
license = "MIT"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
yaml = [
    "ruamel-yaml>=0.18.16",
]

[tool.uv.build-backend]
module-name = "brikk.config"

[build-system]
requires = ["uv_build>=0.8.2,<0.9.0"]
build-backend = "uv_build"
