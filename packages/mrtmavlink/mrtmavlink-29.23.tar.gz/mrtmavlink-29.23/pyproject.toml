[project]
name = "mrtmavlink"
description = "MRT Mavlink Python Bindings"
dynamic = ["version"]
requires-python = ">= 3.8"
dependencies = ["wheel"]
maintainers = [{ name = "Nathan Knotts", email = "nathan@magothyrt.com" }]
readme = "README.md"

[tool.setuptools.dynamic]
version = { attr = "mrtmavlink.__version__" }
