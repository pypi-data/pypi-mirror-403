# Examples

Each script assumes you have installed the local package:

```bash
pip install .
```

- `list_entries.py`: print entry metadata.
- `extract_all.py`: safely extract an archive.
- `stream_read.py`: stream a single entry without loading it all into memory.
