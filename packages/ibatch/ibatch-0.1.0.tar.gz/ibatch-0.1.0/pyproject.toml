[project]
name = "ibatch"
version = "0.1.0"
description = ""
authors = [{ name = "David Polák", email = "email@davidpolak.cz" }]
requires-python = ">=3.13,<4"
readme = "README.md"
license = "GPL-3.0-or-later"
license-files = ["LICENSE"]
classifiers = [
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "click>=8.3.1",
    "geopy>=2.4.1",
    "pyexiftool>=0.5.6",
]

[project.scripts]
ibatch = "ibatch.main:cli"

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
