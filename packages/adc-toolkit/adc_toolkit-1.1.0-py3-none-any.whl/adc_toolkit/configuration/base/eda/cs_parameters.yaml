max_number_of_rows: 100
max_number_of_columns: 50
column_labels: null # Replace with column labels if available, example: ['label1', 'label2']
dataframe_type:
  - pandas.DataFrame # Specify the type of DataFrame
  - pyspark.sql.DataFrame # Allow for Spark DataFrame
summary_statistics: true # Print out of the column name and the statistics
