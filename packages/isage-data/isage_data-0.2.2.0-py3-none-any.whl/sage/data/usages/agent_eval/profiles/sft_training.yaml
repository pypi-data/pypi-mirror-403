name: "sft_training"
description: "SFT training data configuration"
sources:
  sft: agent_sft
  tools: agent_tools
filters:
  split: "train"
parameters:
  max_turns: 12
  batch_size: 32
  shuffle: true
  enable_tool_lookup: true
