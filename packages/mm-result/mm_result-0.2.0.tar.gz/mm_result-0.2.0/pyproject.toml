[project]
name = "mm-result"
version = "0.2.0"
description = "Rust-inspired Result[T] container for functional error handling in Python with optional extra metadata field"
keywords = ["result", "error-handling", "functional", "rust", "monads"]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "bandit~=1.9.3",
    "mypy~=1.19.1",
    "pip-audit~=2.10.0",
    "pre-commit~=4.5.1",
    "pydantic~=2.10",
    "pytest~=9.0.2",
    "pytest-asyncio~=1.3.0",
    "pytest-xdist~=3.8.0",
    "ruff~=0.14.14",
    "ty~=0.0.13",
]

[tool.mypy]
python_version = "3.14"
warn_no_return = false
strict = true
exclude = ["^tests/", "^tmp/"]

[tool.ruff]
line-length = 130
target-version = "py314"
[tool.ruff.lint]
select = ["ALL"]
ignore = [
    "TC",       # flake8-type-checking, TYPE_CHECKING is dangerous, for example it doesn't work with pydantic
    "A005",     # flake8-builtins: stdlib-module-shadowing
    "ERA001",   # eradicate: commented-out-code
    "PT",       # flake8-pytest-style
    "FIX",      # flake8-fixme
    "PLR0911",  # pylint: too-many-return-statements
    "PLR0912",  # pylint: too-many-branches
    "PLR0913",  # pylint: too-many-arguments
    "PLR2004",  # pylint: magic-value-comparison
    "PLC0414",  # pylint: useless-import-alias
    "FBT",      # flake8-boolean-trap
    "EM",       # flake8-errmsg
    "TRY003",   # tryceratops: raise-vanilla-args
    "C901",     # mccabe: complex-structure,
    "BLE001",   # flake8-blind-except
    "S311",     # bandit: suspicious-non-cryptographic-random-usage
    "TD002",    # flake8-todos: missing-todo-author
    "TD003",    # flake8-todos: missing-todo-link
    "RET503",   # flake8-return: implicit-return
    "COM812",   # it's used in ruff formatter
    "ASYNC109",
    "G004",
    "D203",     # pydocstyle: one-blank-line-before-class (conflicts with D211)
    "D213",     # pydocstyle: multi-line-summary-second-line (conflicts with D212)
]
[tool.ruff.lint.pep8-naming]
classmethod-decorators = ["field_validator"]
[tool.ruff.lint.per-file-ignores]
"tests/*.py" = ["ANN", "S", "PLC0415"]
[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.bandit]
exclude_dirs = ["tests"]
skips = ["B311"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
