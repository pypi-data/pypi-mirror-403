[build-system]
requires = ["setuptools >= 69.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flex_dash_docset_generator"
version = "2026.01.25"
authors = [
	{ name="Louie Shprung", email="lshprung@tutanota.com" }
]
description = "Dash docset generator for Flex"
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
	"Operating System :: OS Independent",
	"Programming Language :: Python :: 3"
]
dependencies = [
	"dash_docset_builder>=2026.01.25"
]
license = "GPL-3.0-or-later"
license-files = ["LICENSE"]

[project.scripts]
flex-dash-docset-generator = "flex_dash_docset_generator.main:main"

[project.urls]
Homepage = "https://github.com/lshprung/flex-dash-docset"
Issues = "https://github.com/lshprung/flex-dash-docset/issues"
