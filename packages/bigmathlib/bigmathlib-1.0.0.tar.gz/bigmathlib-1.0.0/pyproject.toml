[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bigmathlib"
version = "1.0.0"
description = "A small, easy-to-use wrapper around Python's Decimal with configurable precision and rounding."
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }

authors = [
  { name = "Jason" }
]

# No external deps – only stdlib `decimal` is used
dependencies = []

[project.urls]
Homepage = "https://github.com/jasonfoley57/bigmathlib"
Source = "https://github.com/jasonfoley57/bigmathlib"
Issues = "https://github.com/jasonfoley57/bigmathlib/issues"

[project.optional-dependencies]
dev = [
  "pytest",
  "build",
  "twine",
]

[tool.setuptools]
packages = ["bigmathlib"]

[tool.setuptools.package-data]
"bigmathlib" = ["*.pem", "*.bin"]