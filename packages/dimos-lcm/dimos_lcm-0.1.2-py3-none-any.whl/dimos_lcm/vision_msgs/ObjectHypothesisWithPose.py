"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys

from . import *
from lcm_msgs import geometry_msgs
from .ObjectHypothesis import ObjectHypothesis


class ObjectHypothesisWithPose(object):
    msg_name = "ObjectHypothesisWithPose"

    __slots__ = ["hypothesis", "pose"]

    __typenames__ = ["ObjectHypothesis", "geometry_msgs.PoseWithCovariance"]

    __dimensions__ = [None, None]

    hypothesis: ObjectHypothesis
    pose: geometry_msgs.PoseWithCovariance

    def __init__(
        self, hypothesis=ObjectHypothesis(), pose=geometry_msgs.PoseWithCovariance()
    ):
        # LCM Type: ObjectHypothesis
        self.hypothesis = hypothesis
        # LCM Type: geometry_msgs.PoseWithCovariance
        self.pose = pose

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(ObjectHypothesisWithPose._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert (
            self.hypothesis._get_packed_fingerprint()
            == ObjectHypothesis._get_packed_fingerprint()
        )
        self.hypothesis._encode_one(buf)
        assert (
            self.pose._get_packed_fingerprint()
            == geometry_msgs.PoseWithCovariance._get_packed_fingerprint()
        )
        self.pose._encode_one(buf)

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        self.hypothesis = cls._get_field_type("hypothesis")._decode_one(buf)
        self.pose = cls._get_field_type("pose")._decode_one(buf)
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        newparents = parents + [cls]
        tmphash = (
            0x65E1D44B451E8A8B
            + ObjectHypothesis._get_hash_recursive(newparents)
            + geometry_msgs.PoseWithCovariance._get_hash_recursive(newparents)
        ) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
