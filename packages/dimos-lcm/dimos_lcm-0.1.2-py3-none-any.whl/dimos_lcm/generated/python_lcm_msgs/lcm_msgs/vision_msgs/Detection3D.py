"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys

from . import *
from lcm_msgs import std_msgs
from .ObjectHypothesisWithPose import ObjectHypothesisWithPose
from .BoundingBox3D import BoundingBox3D


class Detection3D(object):
    msg_name = "vision_msgs.Detection3D"

    __slots__ = ["results_length", "header", "results", "bbox", "id"]

    __typenames__ = [
        "int32_t",
        "std_msgs.Header",
        "ObjectHypothesisWithPose",
        "BoundingBox3D",
        "string",
    ]

    __dimensions__ = [None, None, ["results_length"], None, None]

    results_length: "int32_t"
    header: std_msgs.Header
    results: ObjectHypothesisWithPose
    bbox: BoundingBox3D
    id: "string"

    def __init__(
        self,
        results_length=0,
        header=std_msgs.Header(),
        results=[],
        bbox=BoundingBox3D(),
        id="",
    ):
        # LCM Type: int32_t
        self.results_length = results_length
        # LCM Type: std_msgs.Header
        self.header = header
        # LCM Type: ObjectHypothesisWithPose[results_length]
        self.results = results
        # LCM Type: BoundingBox3D
        self.bbox = bbox
        # LCM Type: string
        self.id = id

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(Detection3D._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">i", self.results_length))
        assert (
            self.header._get_packed_fingerprint()
            == std_msgs.Header._get_packed_fingerprint()
        )
        self.header._encode_one(buf)
        for i0 in range(self.results_length):
            assert (
                self.results[i0]._get_packed_fingerprint()
                == ObjectHypothesisWithPose._get_packed_fingerprint()
            )
            self.results[i0]._encode_one(buf)
        assert (
            self.bbox._get_packed_fingerprint()
            == BoundingBox3D._get_packed_fingerprint()
        )
        self.bbox._encode_one(buf)
        __id_encoded = self.id.encode("utf-8")
        buf.write(struct.pack(">I", len(__id_encoded) + 1))
        buf.write(__id_encoded)
        buf.write(b"\0")

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        self.results_length = struct.unpack(">i", buf.read(4))[0]
        self.header = cls._get_field_type("header")._decode_one(buf)
        self.results = []
        for i0 in range(self.results_length):
            self.results.append(cls._get_field_type("results")._decode_one(buf))
        self.bbox = cls._get_field_type("bbox")._decode_one(buf)
        __id_len = struct.unpack(">I", buf.read(4))[0]
        self.id = buf.read(__id_len)[:-1].decode("utf-8", "replace")
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        newparents = parents + [cls]
        tmphash = (
            0x7C62020C10A78D22
            + std_msgs.Header._get_hash_recursive(newparents)
            + ObjectHypothesisWithPose._get_hash_recursive(newparents)
            + BoundingBox3D._get_hash_recursive(newparents)
        ) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
