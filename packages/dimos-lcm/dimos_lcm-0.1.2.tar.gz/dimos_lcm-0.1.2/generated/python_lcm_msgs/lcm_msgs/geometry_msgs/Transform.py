"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

from io import BytesIO
import struct
import sys

from . import *
from .Vector3 import Vector3
from .Quaternion import Quaternion


class Transform(object):
    msg_name = "geometry_msgs.Transform"

    __slots__ = ["translation", "rotation"]

    __typenames__ = ["Vector3", "Quaternion"]

    __dimensions__ = [None, None]

    translation: Vector3
    rotation: Quaternion

    def __init__(self, translation=Vector3(), rotation=Quaternion()):
        # LCM Type: Vector3
        self.translation = translation
        # LCM Type: Quaternion
        self.rotation = rotation

    def lcm_encode(self):
        buf = BytesIO()
        buf.write(Transform._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert (
            self.translation._get_packed_fingerprint()
            == Vector3._get_packed_fingerprint()
        )
        self.translation._encode_one(buf)
        assert (
            self.rotation._get_packed_fingerprint()
            == Quaternion._get_packed_fingerprint()
        )
        self.rotation._encode_one(buf)

    @classmethod
    def lcm_decode(cls, data: bytes):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != cls._get_packed_fingerprint():
            raise ValueError("Decode error")
        return cls._decode_one(buf)

    @classmethod
    def _decode_one(cls, buf):
        self = cls()
        self.translation = cls._get_field_type("translation")._decode_one(buf)
        self.rotation = cls._get_field_type("rotation")._decode_one(buf)
        return self

    @classmethod
    def _get_field_type(cls, field_name):
        """Get the type for a field from annotations."""
        annotation = cls.__annotations__.get(field_name)
        if annotation is None:
            return None
        if isinstance(annotation, str):
            module = sys.modules[cls.__module__]
            if hasattr(module, annotation):
                return getattr(module, annotation)
            return None
        return annotation

    @classmethod
    def _get_hash_recursive(cls, parents):
        if cls in parents:
            return 0
        newparents = parents + [cls]
        tmphash = (
            0x1275BD1CCBDAF47F
            + Vector3._get_hash_recursive(newparents)
            + Quaternion._get_hash_recursive(newparents)
        ) & 0xFFFFFFFFFFFFFFFF
        tmphash = (
            ((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)
        ) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _packed_fingerprint = None

    @classmethod
    def _get_packed_fingerprint(cls):
        if cls._packed_fingerprint is None:
            cls._packed_fingerprint = struct.pack(">Q", cls._get_hash_recursive([]))
        return cls._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", cls._get_packed_fingerprint())[0]
