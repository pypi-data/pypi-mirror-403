# NOTE: For this file to take effect, move it to:
#   <prefix>/etc/tenzir/plugin/compaction/compaction.yaml

## Note that this example configuration file is verified
## exhaustively, i.e., every config key must contain all
## possible settings. Therefore it also serves as an
## exhaustive reference of supported config options.
#
#time:
#  interval: 1 day
#  step-size: 25
#
#  rules:
#  - after: 7 days
#    name: gdpr_1w
#    pipeline: gdpr_flow
#    preserve-input: true
#    types:
#      - suricata.flow
#      - suricata.netflow
#  - after: 10 days
#    name: gdpr_10d
#    pipeline: uri_clean
#    preserve-input: false
#    types:
#      - zeek.http
#      - suricata.http
#
#space:
#  interval: 1 hour
#  mode: weighted-age
#  disk-budget-high: 10TiB
#  disk-budget-low:   8TiB
#  scan-binary: ""
#  step-size: 25
#
#  weights:
#    - weight: 0.01
#      pipeline: aggregate_flows
#      types: [suricata.flow]
#    - weight: 10
#      pipeline: delete
#      types: [suricata.alert]
#