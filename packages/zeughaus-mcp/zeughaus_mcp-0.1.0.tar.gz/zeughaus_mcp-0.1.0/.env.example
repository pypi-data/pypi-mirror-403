# Zeughaus MCP Server Configuration

# Base URL of your private Nixery registry
# Example: docker.my-company.com/nixery-cache
NIXERY_REGISTRY_URL=docker.my-company.com/nixery-cache

# Absolute path on the host to the workspace directory
# This directory will be mounted read/write into containers at /workspace
HOST_WORKSPACE_ROOT=/path/to/workspace

# Docker network mode for spawned containers
# Options: host, bridge, none, or a custom network name
DOCKER_NETWORK_MODE=host

# Port for the MCP SSE server
MCP_SERVER_PORT=8000

# Maximum execution time for containers in seconds
# Set to 0 for no timeout (not recommended)
CONTAINER_TIMEOUT_SECONDS=300
