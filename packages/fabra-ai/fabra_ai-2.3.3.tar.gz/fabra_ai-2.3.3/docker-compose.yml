services:
  fabra:
    build: .
    ports:
      - "8005:8000"
    environment:
      - FABRA_REDIS_URL=redis://redis:6379
      - FABRA_POSTGRES_URL=postgresql://user:password@postgres:5432/fabra
      - FABRA_ENV=production
    depends_on:
      - redis
      - postgres
    command: fabra serve examples/basic_features.py --host 0.0.0.0 --port 8000

  redis:
    image: redis:7
    ports:
      - "6380:6379"

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: fabra
    ports:
      - "5433:5432"

  prometheus:
    image: prom/prometheus
    ports:
      - "9095:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
