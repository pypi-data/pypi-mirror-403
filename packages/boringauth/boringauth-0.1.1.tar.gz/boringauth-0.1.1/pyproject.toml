[project]
name = "boringauth"
version = "0.1.1"
description = "Boring, predictable FastAPI auth generator"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

authors = [
  { name = "Feroz", email = "feroz-chand@outlook.com" }
]

[project.scripts]
boringauth = "boringauth.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
