# configuration file /etc/nginx/nginx.conf:
http {
  server {
    location /example_service {
        include /tmp/deny;
        deny all;
    }
  }
}

# configuration file /tmp/deny:
allow 127.0.0.1;
