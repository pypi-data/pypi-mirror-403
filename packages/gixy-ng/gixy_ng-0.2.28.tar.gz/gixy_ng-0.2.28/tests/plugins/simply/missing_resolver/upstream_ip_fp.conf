# Should NOT trigger: upstream server uses IP address
http {
    upstream backend {
        server 192.168.1.100:8080;
    }

    server {
        location / {
            proxy_pass http://backend;
        }
    }
}
