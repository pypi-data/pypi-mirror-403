# Should trigger: upstream server without resolve parameter
http {
    upstream backend {
        server example.com;
    }

    server {
        location / {
            proxy_pass http://backend;
        }
    }
}
