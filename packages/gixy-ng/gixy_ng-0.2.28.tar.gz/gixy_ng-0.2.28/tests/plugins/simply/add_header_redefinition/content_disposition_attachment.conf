# Test: Dropping Content-Disposition: attachment should be MEDIUM severity
# attachment prevents inline execution of potentially malicious content (XSS prevention)
server {
    add_header Content-Disposition "attachment; filename=download.pdf";

    location /downloads/ {
        add_header X-Download-Ready "true";
        # Content-Disposition: attachment is now DROPPED - security issue!
    }
}
