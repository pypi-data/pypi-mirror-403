# Test: Dropping Pragma: no-cache should be MEDIUM severity
# Legacy cache control header with security implications
server {
    add_header Pragma "no-cache";
    add_header Cache-Control "no-store";

    location /sensitive/ {
        add_header X-Custom "header";
        # Both Pragma and Cache-Control are DROPPED - security issue!
    }
}
