# Test: Dropping Cache-Control: no-store should be MEDIUM severity
# This is a security regression - sensitive data could be cached
server {
    add_header Cache-Control "no-store";

    location /private/ {
        add_header X-Custom "value";
        # Cache-Control: no-store is now DROPPED - security issue!
    }
}
