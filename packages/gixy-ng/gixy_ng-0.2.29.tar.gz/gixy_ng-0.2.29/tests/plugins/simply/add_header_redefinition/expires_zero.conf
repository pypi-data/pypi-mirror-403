# Test: Dropping Expires: 0 should be MEDIUM severity
# Expires: 0 means "already expired" - security intent to prevent caching
server {
    add_header Expires "0";

    location /auth/ {
        add_header X-Auth-Status "ok";
        # Expires: 0 is now DROPPED - security issue!
    }
}
