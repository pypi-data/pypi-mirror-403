# Test: Dropping Cache-Control: private should be MEDIUM severity
# "private" prevents proxy caching of user-specific data
server {
    add_header Cache-Control "private, no-cache";

    location /api/ {
        add_header X-Request-Id "123";
        # Cache-Control with private is now DROPPED - security issue!
    }
}
