[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "semantic-retrieval-data"
version = "0.1.0"
dependencies = [
    "mcp",
    "httpx",
    "pyyaml",
]

[project.scripts]
semantic-retrieval-data = "mcp_server:main"
