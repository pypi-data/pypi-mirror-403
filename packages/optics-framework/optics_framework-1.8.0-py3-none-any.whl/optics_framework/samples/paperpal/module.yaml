Modules:
  - Launch Application:
    - Launch App
  - Authenticate User:
    - Enter Text ${Email_element} ${email}
    - Press Element ${Next_button}
    - Assert Presence ${Password_element}
    - Enter Text ${Password_element} ${password}
    - Press Element ${Login_button}
    - assert Presence ${ChatPDF_button}
  - Navigate to ChatPDF's history:
    - Press Element ${ChatPDF_button}
    - Press Element ${Sample_document}
  - summerize the document:
    - Assert Presence ${Prompt_element}
    - Enter Text ${Prompt_element} ${Prompt}
    - Enter Text ${Prompt_element} ${Submit_button}
    - Sleep ${duration}
    - Assert Presence ${Final_assertion}
    - Enter Text Using Keyboard  
    - Read Data <API> --enable excyption
    - Read Data (calenday_dat)+90
