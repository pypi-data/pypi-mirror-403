[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "claudit"
version = "0.0.1"
description = "Reserved package name"
readme = {text = "This package name is reserved.", content-type = "text/plain"}
requires-python = ">=3.13"

[tool.hatch.build.targets.wheel]
packages = ["src/claudit"]
