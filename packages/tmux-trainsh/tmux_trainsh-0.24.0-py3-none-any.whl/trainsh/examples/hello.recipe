# hello-world
# A simple recipe to test trainsh locally

var MESSAGE = Hello from trainsh

tmux.open @local as hello

@hello > echo "$MESSAGE"

# Notify when complete
notify "$MESSAGE"

tmux.close @hello
