import pandas as pd
from solas_disparity.utils import pgrg_ordered as pgrg_ordered
from typing import Any, Literal

def validate_group_arguments(group_data: pd.DataFrame, protected_groups: list[str], reference_groups: list[str], group_categories: list[str]) -> None: ...
def validate_column_arguments(group_data: pd.DataFrame, column: pd.Series, column_name: Literal['outcome', 'label', 'prediction', 'segment'], column_type: Literal['boolean', 'categorical', 'continuous'], category_order: list[Any] | None = None) -> None: ...
def validate_sample_weight_arguments(group_data: pd.DataFrame, sample_weight: pd.Series | None) -> None: ...
