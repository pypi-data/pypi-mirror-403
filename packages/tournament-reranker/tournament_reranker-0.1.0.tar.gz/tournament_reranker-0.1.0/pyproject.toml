[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[project]
name = "tournament-reranker"
version = "0.1.0"
description = "LLM reranking via a tournament/pyramid strategy."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
openai = ["openai>=2.14.0"]

[tool.hatch.build.targets.wheel]
packages = ["tournament_reranker"]
