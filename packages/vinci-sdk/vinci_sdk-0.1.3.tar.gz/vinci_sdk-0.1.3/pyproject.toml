[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vinci-sdk"
version = "0.1.3"
description = "The Leonardo Engine"
authors = [{name = "Davide", email = "davide@example.com"}]
requires-python = ">=3.8"
dependencies = []
[tool.hatch.build.targets.sdist]
include = [
  "src/vinci",
  "pyproject.toml",
  "README.md"
]


[tool.hatch.build.targets.wheel]
packages = ["src/vinci"]
