# content of pytest.ini
[pytest]
testpaths = tests
markers =
    skip_debug: skip test if PPIO_DEBUG is set
    unit: unit tests
    integration: integration tests
    network: tests requiring network access
    slow: slow running tests
    asyncio: async tests
asyncio_mode = auto
addopts = --import-mode=importlib --asyncio-mode=auto
