[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "area-lib-mehak"
version = "0.1.2"
description = "A simple library to calculate areas of shapes like circle, triangle, square"
authors = [{ name="Mehak" }]
readme = "README.md"
license = { text="MIT" }
dependencies = []
