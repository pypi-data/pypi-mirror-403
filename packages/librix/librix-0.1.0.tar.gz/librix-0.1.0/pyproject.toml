[project]
name = "librix"
version = "0.1.0"
description = "A professional API and scraper for Anna's Archive"
authors = [
    { name = "Kosma Gąsiorowski", email = "kosma@example.com" }
]
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "fastapi>=0.109.0",
    "uvicorn>=0.27.0",
]
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.scripts]
librix = "librix.api:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
