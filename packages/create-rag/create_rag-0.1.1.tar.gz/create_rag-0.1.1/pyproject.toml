[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "create-rag"
version = "0.1.1"
description = "CLI tool to scaffold RAG projects"
authors = [{ name = "Your Name" }]
dependencies = []

[project.scripts]
create-rag = "create_rag.cli:main"
