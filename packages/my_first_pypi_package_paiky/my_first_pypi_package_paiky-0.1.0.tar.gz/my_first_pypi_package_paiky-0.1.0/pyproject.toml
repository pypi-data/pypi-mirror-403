[project]
name = "my_first_pypi_package_paiky"
version = "0.1.0"
description = "Made with love by Constructor"
authors = [
    {name = "paiky",email = "dev-miguel-mendez@proton.me"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "my_first_pypi_package_paiky", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
