{% for service in services %}
export * from './{{ service.filename }}';
import { {{ service.name }} } from './{{ service.filename }}';
{% endfor %}
export const APIS = [{{ services | map(attribute='name') | join(', ') }}];
