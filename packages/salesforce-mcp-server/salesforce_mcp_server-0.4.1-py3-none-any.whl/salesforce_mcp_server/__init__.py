"""Salesforce MCP Server - Multi-user OAuth PKCE support using simple-salesforce."""

try:
    from salesforce_mcp_server._version import __version__
except ImportError:
    __version__ = "0.0.0"  # fallback for editable installs without build
