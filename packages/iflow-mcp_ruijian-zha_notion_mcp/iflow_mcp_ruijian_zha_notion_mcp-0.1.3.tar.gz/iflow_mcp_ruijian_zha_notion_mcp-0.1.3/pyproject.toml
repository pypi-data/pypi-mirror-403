[project]
name = "iflow-mcp_ruijian-zha_notion_mcp"
version = "0.1.3"
description = "Notion MCP integration for todo lists"
requires-python = ">=3.10"
dependencies = [
    "mcp",
    "httpx",
]
[project.scripts]
notion_mcp = "iflow_mcp_ruijian_zha_notion_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
sources = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"