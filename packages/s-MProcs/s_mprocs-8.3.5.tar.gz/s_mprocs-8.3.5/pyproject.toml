[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "s_MProcs"
version = "8.3.5"
description = "A Life program by tderk, originally LPro.py and Destiny [2024] -this version does not save to a .txt file"
requires-python = ">=3.8"
license = { file = "LICENSE" }
authors = [{ name = "tderk", email = "usvu.tech@gmail.com" }]
keywords = ["life", "hack", "destiny", "meta", "program"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.scripts]
s-MProcs = "s_MProcs.scripts.s_MProcs:main"
        
[tool.setuptools]
package-dir = {"" = "src"}

