// Shared health check test for all Node/NestJS-based RapidKit kits.
// This file should be included in every kit's test/ directory as health.spec.ts.j2

describe('Health', () => {
  it('should always pass (template placeholder)', () => {
    expect(true).toBe(true);
  });
});
