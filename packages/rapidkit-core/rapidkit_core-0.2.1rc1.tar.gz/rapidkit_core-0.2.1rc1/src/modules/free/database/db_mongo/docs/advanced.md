# Advanced Topics

Capture extensibility hooks, override patterns, and performance considerations.

## Overrides

Generation-time overrides are applied through `overrides.py` using `RAPIDKIT_DB_MONGO_*` variables.

For production, tune pool sizes/timeouts and enable TLS as required by your deployment.
