{# Jinja snippet used to inject shared fragments into generated artefacts. #}
{# Name this snippet when referencing from module.yaml generation entries. #}

{% set module_name = "db_mongo" %}

# {{ module_title }} scaffolding
# This snippet is reserved for future structured injections. It intentionally renders
# a clearly identifiable banner so downstream projects can anchor additional content.
