# Advanced Topics

Capture extensibility hooks, override patterns, and performance considerations.

## Overrides

Generation-time overrides are exposed in `README.md` (e.g. `RAPIDKIT_STRIPE_MODE`, retry policy
knobs).

For production, wire webhook signature verification and idempotency at your HTTP boundary.
