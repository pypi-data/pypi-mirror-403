[project]
name = "{{project_name}}"
version = "0.1.0"
description = "{{project_name}} - A noot-tested CLI project"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["noot", "pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
