[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "framex-ce"
version = "0.1.0"
description = "Quick-start framework for pygame-ce prototypes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pygame-ce"]

authors = [{ name = "JK" }]
