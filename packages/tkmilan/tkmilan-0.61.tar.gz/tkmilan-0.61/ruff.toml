# See also `setup.cfg`
# Use `flake8-to-ruff setup.cfg >ruff.toml` to regenerate this
ignore = [
    # Long lines
    "E501",
]
select = [
    "E", "F", "W",  # Default
    'UP031', 'UP032',  # flake8-use-fstring (partial)
]

