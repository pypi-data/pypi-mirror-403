[build-system]
requires = [
    "setuptools>=60",
    "wheel",
    "setuptools_scm[toml]>=6.4.2",
    # TODO: Migrate to https://pypi.org/project/versioningit/
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
