# Root Cube: TEXT.PROMPT.LLM
# Its structure IS the schema definition (Living Prototype)
name: TYPE_TEXT_PROMPT_LLM
typology: TEXT.PROMPT.LLM
is_type_root: true
causality: TYPE_TEXT_PROMPT

temporal: null      # optional (auto-filled by Storage)
ontology: {}        # optional
causality: null     # optional

individuality:
  prompts:
    system: ""        # REQUIRED
    user: ""          # REQUIRED
    context: null     # optional
  model:
    provider: "openai"      # optional with default
    name: "gpt-4"           # optional with default
    temperature: 0.7        # optional with default
    max_tokens: 4096        # optional with default
  output:
    mime_type: "text/plain" # optional with default
    schema: null            # optional
  meta: {}                  # optional
