[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tinygent-mistralai"
version = "0.1.1007"
dependencies = [
    "mistral-common>=1.8.5",
    "mistralai>=1.9.11",
    "tinygent",
    "transformers>=4.57.6",
]

[project.entry-points.components]
tiny_mistralai = "tiny_mistralai.register"

[tool.hatch.build.targets.wheel]
packages = ["src/tiny_mistralai"]

[tool.uv.sources]
tinygent = { workspace = true }
