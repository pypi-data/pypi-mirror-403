# coding: utf-8

"""
    Fastify Template API

    API documentation using Swagger

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class PresentationConfig(BaseModel):
    """
    PresentationConfig
    """ # noqa: E501
    text_amount: StrictStr = Field(alias="textAmount")
    image_source: StrictStr = Field(alias="imageSource")
    num_cards: Union[StrictFloat, StrictInt] = Field(alias="numCards")
    dimensions: StrictStr
    additional_instructions: Optional[StrictStr] = Field(default=None, alias="additionalInstructions")
    __properties: ClassVar[List[str]] = ["textAmount", "imageSource", "numCards", "dimensions", "additionalInstructions"]

    @field_validator('text_amount')
    def text_amount_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['brief', 'medium', 'detailed', 'extensive']):
            raise ValueError("must be one of enum values ('brief', 'medium', 'detailed', 'extensive')")
        return value

    @field_validator('image_source')
    def image_source_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['aiGenerated', 'placeholder', 'noImages']):
            raise ValueError("must be one of enum values ('aiGenerated', 'placeholder', 'noImages')")
        return value

    @field_validator('dimensions')
    def dimensions_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['fluid', '16x9', '4x3']):
            raise ValueError("must be one of enum values ('fluid', '16x9', '4x3')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PresentationConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PresentationConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "textAmount": obj.get("textAmount"),
            "imageSource": obj.get("imageSource"),
            "numCards": obj.get("numCards"),
            "dimensions": obj.get("dimensions"),
            "additionalInstructions": obj.get("additionalInstructions")
        })
        return _obj


