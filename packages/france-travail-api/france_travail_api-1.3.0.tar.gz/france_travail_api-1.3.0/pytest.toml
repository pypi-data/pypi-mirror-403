[pytest]
markers = [
    "e2e: end-to-end tests. Those are tests for public APIs.",
    "integration: integration tests. Those are component-scopes tests with volatile dependencies like HTTP clients.",
]