[build-system]
requires = ["setuptools", "wheel", "Cython"]
build-backend = "setuptools.build_meta"

[project]
name = "sgFCMed"
version = "0.0.2"
description = "A library for String Grammar Fuzzy C-Medians"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
  { name = "Computational Intelligence Research Laboratory", email = "cilabcmu@gmail.com" }
]
dependencies = ["pytest"]

[tool.setuptools.packages.find]
where = ["."]