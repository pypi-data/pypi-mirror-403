[build-system]
build-backend = 'setuptools.build_meta'
requires = [
    'setuptools>=64',
    'numpy',
    ]

[tool.pytest.ini_options]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "interactive: mark tests with X11 interactive plots",
    "serial",
]