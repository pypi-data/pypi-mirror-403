[project]
name = "gs_prompt_manager"
version = "0.0.1"
authors = [
  { name="Guan Huang" },
]
description = "A small package for in-memory prompt management against python file"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "Apache-2.0"
license-files = ["LICEN[CS]E*"]

[project.urls]