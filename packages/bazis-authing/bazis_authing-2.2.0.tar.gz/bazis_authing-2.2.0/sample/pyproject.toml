[project]
name = "sample"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "bazis-authing",
    "bazis-test-utils",
]

[tool.uv.sources]
bazis-authing = { path = "../", editable = true }

[tool.pytest.ini_options]
django_settings_module = "sample.settings"
