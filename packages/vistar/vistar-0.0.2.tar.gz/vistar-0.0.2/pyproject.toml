[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vistar"
version = "0.0.2"
description = "A toolkit under development by Riemannian Vista."
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.10"
authors = [{ name = "Marisha Singh" }]

[tool.hatch.build.targets.wheel]
packages = ["src/vistar"]
