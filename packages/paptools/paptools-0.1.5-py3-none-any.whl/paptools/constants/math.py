from ..core.number import Number
import numpy as np
import sympy as sp

pi = Number(np.pi, None, symbol="pi", unit=None)
e = Number(np.e, None, symbol="e", unit=None)
tau = Number(2 * np.pi, None, symbol="tau", unit=None)
