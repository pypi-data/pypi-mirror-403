# 测试环境配置文件
# 这个文件用于测试时的配置

# MongoDB配置
mongo:
  # 无认证的本地连接，适用于测试
  test: "mongodb://localhost:27017/test"
  # 如果需要认证，可以使用以下格式
  # test_auth: "mongodb://username:password@localhost:27017/test?authSource=admin"

# Redis配置
redis:
  test: "redis://localhost:6379"
  cache: redis://127.0.0.1:6379/1 
