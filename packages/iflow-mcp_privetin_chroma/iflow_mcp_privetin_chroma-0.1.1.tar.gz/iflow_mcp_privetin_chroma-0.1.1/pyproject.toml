[project]
name = "iflow-mcp_privetin_chroma"
version = "0.1.1"
description = "A MCP server project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [ "mcp>=1.1.2", "chromadb>=0.4.22", "sentence-transformers>=2.2.2"]
[[project.authors]]
name = "privetin"
email = "81558906+privetin@users.noreply.github.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/chroma"]

[project.scripts]
chroma-mcp = "chroma:main"