[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "moss-crewai"
version = "1.0.0"
description = "MOSS cryptographic signing integration for CrewAI"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "BSL-1.1"}
authors = [
    {name = "IAMPASS Technologies", email = "moss@iampass.com"}
]
keywords = ["moss", "crewai", "cryptography", "signing", "ai-agents", "security"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: Other/Proprietary License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "moss-sdk>=1.0.0",
    "crewai>=0.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "ruff>=0.1.0",
]

[project.urls]
Homepage = "https://mosscomputing.com"
Documentation = "https://mosscomputing.com/docs"
Repository = "https://github.com/mosscomputing/moss-crewai"

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.hatch.build.targets.wheel]
packages = ["moss_crewai"]

[tool.ruff]
target-version = "py39"
line-length = 100
