# rscm-two-layer

Two-layer climate model component for RSCM.

This crate provides an implementation of a two-layer energy balance model that can be used within the RSCM framework
to simulate global temperature response to radiative forcing.

## Installation

Add this to your `Cargo.toml`

```toml
[dependencies]
rscm-two-layer = "0.3"
```
