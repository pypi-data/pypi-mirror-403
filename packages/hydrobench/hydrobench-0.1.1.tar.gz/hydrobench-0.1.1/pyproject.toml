[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hydrobench"
version = "0.1.1"
description = "Hydrological benchmarking toolkit"
requires-python = ">=3.10"
authors = [{ name = "Mirko Mälicke", email = "mirko@hydrocode.de"}]
dependencies = [
    "pocketbase",
    "polars>=1.37.1",
]
