# Simple VPC-only preset
#
# Enables the VPC module with sane defaults and leaves other modules disabled.

name: simple-vpc
region: us-west-2
environment: dev

vpc:
  enabled: true

# EKS, ALB, and services are left at their defaults (disabled / empty)
