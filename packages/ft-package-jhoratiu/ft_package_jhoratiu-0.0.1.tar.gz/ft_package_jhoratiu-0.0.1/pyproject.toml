[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package_jhoratiu"
version = "0.0.1"
description = "A sample test package"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.7"
classifiers = []

[tool.setuptools]
packages = ["ft_package"]
package-dir = {"" = "src"}
