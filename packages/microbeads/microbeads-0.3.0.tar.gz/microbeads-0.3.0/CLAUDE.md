# Microbeads

See [AGENTS.md](AGENTS.md) for project instructions.

## Code Quality (Before Every Commit)

```bash
uv run ruff check src tests --fix   # Fix lint errors
uv run ruff format src tests        # Format code
uv run pytest                       # Run tests
```
