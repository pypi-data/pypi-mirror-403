{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "the input for the task",
  "type": "array",
  "required": ["source", "target"],
  "items": {
    "$comment": "single input mapping object",
    "type": "object",
    "properties": {
      "source": {
        "type": "string",
        "pattern": "/",
        "$comment": "source path (e.g. 'baijia-login-checker/data')"
      },
      "target": {
        "type": "string",
        "pattern": "^bizdata/",
        "$comment": "target path (e.g. 'bizdata/should_skip')"
      },
      "before_map": {
        "type": "string",
        "pattern": "^lambda\\s+[a-zA-Z_][a-zA-Z0-9_]*:\\s+.+$",
        "$comment": "lambda function string（e.g. 'lambda x: not x'），syntax must follow lambda spec"
      }
    },
    "additionalProperties": false
  },
  "minItems": 1,
  "uniqueItems": true
}