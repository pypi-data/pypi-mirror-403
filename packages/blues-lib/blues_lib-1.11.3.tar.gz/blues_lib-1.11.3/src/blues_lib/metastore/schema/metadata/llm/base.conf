{
  "tpl_id":"metadata.llm.base", // instance id
  
  "summary":{},

  "request":{ 
    "body":"{{request_body}}", // llm api request body
  },
  "prompt":{
    "max_chars":"{{max_chars:-2500}}", // 最大输入字符数
    "user":{
      "prefix":"{{user_prefix}}",
      "suffix":"{{user_suffix}}", 
      "content":"{{user_content}}",
    },
    "system":{
      "content":"{{system_content}}",
    }
  }
}