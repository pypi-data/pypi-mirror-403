[project]
name = "emmel"
version = "0.0.1"
description = "M language (Power Query) parser and evaluator"
license = "MIT"
readme = "README.md"
requires-python = ">=3.11"

[project.urls]
repository = "https://github.com/onnovalkering/emmel"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
