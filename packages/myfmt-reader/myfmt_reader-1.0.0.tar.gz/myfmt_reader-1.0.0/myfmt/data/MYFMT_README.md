# 📄 MyFmt (.myfmt) Format Guide

## Introduction
**.myfmt** is a human-readable, structured data format designed for easy integration with **Pandas**. It combines the simplicity of INI files with the structure of JSON.

## Format Rules

### Meta Section *(Optional)*